# Nota: reconciliação de `schema_migrations` para 20260924203300

**Status: já resolvido em produção. Nenhuma ação pendente.**

## O que foi investigado

Uma investigação anterior nesta sessão sinalizou drift entre os arquivos de
migration do repositório e a tabela de controle
`supabase_migrations.schema_migrations`: 458 arquivos em
`supabase/migrations/` (excluindo os subdiretórios `_foreign/` e
`_superseded/`) contra 457 linhas na tabela, com a migration
`20260924203300_prevent_contact_assignee_hijack.sql` ausente — mesmo com o
trigger `trg_prevent_contact_assignee_hijack` e a função
`prevent_contact_assignee_hijack()` já aplicados em produção. Isso derrubava
o job "Comparar migrations com schema_migrations" do workflow **DB Live
Guard**.

## Revalidação (somente leitura, banco Supabase Cloud "ZAPP WEB V2")

| Verificação | Resultado |
|---|---|
| `count(*)` em `schema_migrations` | **458** |
| `EXISTS(... version = '20260924203300')` | **true** |
| Arquivos `.sql` em `supabase/migrations/` (GitHub, `main`) | **458** (460 entradas − 2 diretórios) |
| `statements[1]` da linha vs. arquivo do repo vs. `pg_get_functiondef()` ao vivo | **idênticos** |
| Run mais recente de "DB Live Guard" em `main` (id `36069398787`, push, `2026-09-24T22:47:44Z`) | **success** |

Antes disso, o histórico de runs de "DB Live Guard" mostra falhas
consecutivas entre `~20:48Z` e `~22:39Z` — consistente com a janela em que o
drift esteve ativo. A partir de `22:47:44Z` (mesmo minuto em que a PR #649
`automation/types-sync`, não relacionada a esta tabela, foi mergeada) os runs
passam a ficar verdes.

## Conclusão

O drift **já foi corrigido em produção**, fora desta sessão, muito
provavelmente por um `supabase db push` real (que registra
`schema_migrations` corretamente, ao contrário de uma aplicação isolada via
MCP/`db_apply_migration`). **Nenhum INSERT foi executado por este agente** —
apenas leituras (`SELECT`, `describe_table`, listagens) contra o banco.

## Por que este script ainda existe

`reconcile-schema-migrations-20260924203300.sql` fica versionado como rede de
segurança **idempotente** (`INSERT ... WHERE NOT EXISTS`) para o caso
específico já diagnosticado se ele se repetir (reset de branch, restore de
snapshot anterior a `2026-09-24T22:4xZ`, ou nova aplicação do trigger via MCP
sem `supabase db push`). Rodar contra o estado atual é um no-op seguro.
**Aguardando aprovação humana** antes de qualquer execução — não é para
rodar automaticamente por este PR.
