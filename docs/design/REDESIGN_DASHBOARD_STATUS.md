# Redesign Dashboard — Navy Operational — STATUS
Branch: redesign/dashboard-navy-v1 · Base: 661219c0 (origin/redesign/contatos-navy-v2 no momento da criação do worktree; origin avançou para fa3984f1 durante a execução — registrado como divergência) · Worktree: /workspace/repos/Zapp_Web_V2-dashboard · Preview: vite preview :4174 · QA: /workspace/qa-dashboard · QA user: ok (etapa 6 — `qa.visual@promobrindes.com.br`, login via `/auth/v1/token?grant_type=password` no Supabase Cloud `tnnnlkbymytvtqngbbqh` retorna `access_token`) · Disco: 35G livres antes / 35G depois (etapa 3; 634 pacotes instalados em 2.40s, sem impacto mensurável no `df -h`)

## CP0 Ambiente        [x] sha=b462174b · before=/workspace/qa-dashboard/out/00-before-1583.png,00-before-1920.png (produção, tema pré-navy — esperado, main ainda não tem Contatos) · gates baseline: typecheck=0 lint-ratchet=OK(baseline=1223,atual=1216,novas=0) tc-ratchet=OK(baseline=0,atual=0,novas=0) implicit=OK(0,baseline=0) vitest=sem arquivos de teste em src/components/dashboard, src/hooks/analytics, src/hooks/dashboard ainda (esperado — exit 1 "No test files found", não é regressão; testes chegam nas Fases 4-8) · porta 4174 livre · QA em /workspace/qa-dashboard pronto (shot.mjs/measure.mjs adaptados p/ ?view=dashboard, colors.mjs genérico)
## CP1 Fundação        [x] sha=af3347f8 · shot=01-after-1583.png (retirado às 10:38 — o primeiro shot de 10:24 era inválido, ver Divergências) · gutter=16 sidebar=234 (1583 e 1920, scrollW=innerW nos dois, sem overflow horizontal) · tops dos blocos (px, alvo=referência §2.1 +3, Δ): header=36(alvo36,Δ0) tabs=116(alvo116,Δ0) banner=166(alvo156,Δ10) kpi=249(alvo237,Δ12) row2=354(alvo343,Δ11) row3=623(alvo611,Δ12) row4=853(alvo841,Δ12) — resíduo documentado abaixo · bg=ΔE0.00 (token, amostra real do screenshot) · card=ΔE0 (token `--card` confirmado via probe `getComputedStyle` de `.bg-card` = rgb(13,23,38); o header do dashboard ainda não pinta `bg-card` nesta fase — chega na etapa 21/Fase 2, então não há card visual do módulo para amostrar ainda) · gates: typecheck=0 lint-ratchet=OK(1223→1216,novas=0) tc-ratchet=OK(0,novas=0) implicit-any=OK(0) vitest=sem arquivos ainda (esperado, igual CP0) · 9 tabs renderizam e trocam de conteúdo (`overview`→6 filhos, `analytics`→2, `goals`/`sla`/`team`/`satisfaction`/`sentiment`/`reports`→1 cada, `ai`→2) — checado com clique programático nas 9; grep `Aurora|Particles|Parallax|blur(|neon|glow-gradient` em `DashboardView.tsx` = 0; grep cores hardcoded (`hsl(240|#hex|rgb(`) em `DashboardView.tsx`+`DashboardFilters.tsx` = 0
## CP2 Header          [x] sha=7bc6c4bd · shot=02-after-1583.png · topBar=28(alvo28±4) headerCard=70(alvo70±4) tile=40(alvo40±2) title=24px(alvo24±1) period=167x34(alvo167x34±4) queue=135x34(alvo135x34) agent=158x34(alvo158x34) refresh=38x34(alvo38x34) · ΔE headerTile=0.00(#0e285d) headerCardBg=0.00(#0d1726) refresh=8.81(alvo≤8, #133470 — bg-primary/25 é alpha-blend sobre o card, não swatch flat; diferença sub-perceptível, registrada sem forçar) · responsivo: 1583/1920 ok, <xl(1024/1200) header vira h-auto(113px) e filtros quebram linha, <md(390/700) chip de período w-full(332/642px), sem overflow em nenhum (scrollW=innerW) · 4 filtros testados funcionalmente (período→"Ontem"/domingo 06 de set correto, fila 2 opções, agente 6 opções, refresh→spin ativa), 0 console errors
## CP3 Tabs            [x] sha=74671be9 · shot=03-after-1583.png · pill=32px(alvo32±2) activeW=116px(alvo≈139±12 → fora por 11px, ver Divergências) oneLine@1583=true @1920=true · footprint 9 tabs=1071px (gate "≤1317" ok, referência informal 1135) · ΔE pill=0.00 (#146eff, após fix `isolate` — ver Divergências) · 9 tabs clicam e trocam de painel corretamente
## CP4 Banner          [ ] sha= · shot=04-after-1583.png · banner=_ avatar=_ levelTile=_ chips=_ · agent_stats(QA user)=xp:_ level:_ streak:_ achievements:_
## CP5 KPIs            [ ] sha= · shot=05-after-1583.png · kpi=[_,_,_,_,_] widths=_ tile=_ valueFont=_ · tiles ΔE=_ · resolvedToday(closures)=_ vs stats.resolvedToday=_
## CP6 Linha 2         [ ] sha= · shot=06-after-1583.png · row2=_ row2W=_ tile44=_ volumePlot=_ donut=_ nowRows=_ · ΔE violet=_ arco=_ · capacidade: omitida · SLA rótulo usado: _
## CP7 Linha 3         [ ] sha= · shot=07-after-1583.png · row3=_ row3W=_ tableRow=_ badge=_ activityRow=_ teamRow=_ verTodas=_ · events embed: sim|não
## CP8 Linha 4         [ ] sha= · shot=08-after-1583.png · compare=08-compare.png · row4=_ iaTile=_ csatBars=_ tile34=_ · ΔE amber=_ stars=_ csat5=_ csat3=_ · chip Ativo: fonte|omitido
## CP9 Preservação     [ ] sha= · shots=09-<9 tabs>.png,09-regress-{contacts,inbox,pipeline}.png · arquivos sem uso: _
## CP10 Motion         [ ] sha= · reduced-motion: durations=0 · bundle Δ=_ KB gz
## CP11 Fidelidade     [ ] final=11-final-1583.png,11-final-1920.png · geometria: N/N ok (1583) N/N ok (1920) · cores: N/N · mismatch%: topbar=_ header=_ tabs=_ banner=_ kpi=_ row2=_ row3=_ row4=_ · 1440/1366/mobile/light ok
## CP12 Entrega        [ ] PR=<url> (base=_) · CI=_ · contatos mergeado: sim|não · rebase=_ · merge=<sha> · prod=12-prod-1920.png · colors prod ok

## Divergências plano × código
- Etapa 1 do plano previa `git worktree add` a partir de `Zapp_Web_V2`; o worktree e a branch já existiam antes desta sessão (feitos por Joaquim/sessão anterior). Verificado com `git worktree list` e `git log -1`: worktree em `/workspace/repos/Zapp_Web_V2-dashboard`, branch `redesign/dashboard-navy-v1`, HEAD `36e3b740`, merge-base com `origin/redesign/contatos-navy-v2` = `661219c0`. Não refeito, apenas confirmado.
- `docs/design/dashboard-reference.png` e `docs/design/PROMPT_MESTRE_DASHBOARD_WIDE_24_FULLHD.md` **não existem** em `origin/main` (`git cat-file -e` retornou 128 para os dois). E.4: **pulado** (sem PNG no repo). Prompt mestre: seguindo apenas este plano (seção 2 é a fonte executável, conforme o próprio plano prevê).
- Base real no momento da execução: `origin/redesign/contatos-navy-v2` avançou para `fa3984f1` (commit `36e3b740` foi feito sobre `661219c0`). Não fazer rebase agora — plano só pede rebase na etapa 99, após merge de Contatos.
- Etapa 9 (auditoria rápida): todas as 5 divergências do plano (seção 0.2) confirmadas byte a byte no código real em 07/09: `AnimatedBadge value="1.250"` e `LevelProgress currentXP={1250} requiredXP={2000} level={12}` em `DashboardView.tsx:118,124`; `currentCapacity = 35` em `useDemandPrediction.ts:52`; zero ocorrências de `capacity|max_concurrent` em `types.ts`; `waitingCount: 0` (linha 33) e `recentActivity: []` (linha 48) em `useDashboardData.ts`; `WidgetConfigSheet.tsx`/`DraggableWidgetContainer.tsx` sem uso fora de si mesmos. Nenhuma divergência nova encontrada — plano bate com o código.
- Etapa 15: o plano pede "placeholders vazios" para topbar/header/tabs junto com banner/kpis/row2-4/gamification. Decisão: topbar (novo, sem regressão possível) ficou vazio; header e tabs ficaram **funcionais** (DashboardFilters real + TabsList real controlando os 9 TabsContent), não vazios — para não quebrar filtros/troca de aba a cada commit intermediário (Regra 5, zero regressão funcional, é contrato até o CP12, mas evitar regressão a cada push reduz risco). Banner/KPIs/row2/row3/row4/gamification ficaram vazios (`min-h-*` sem conteúdo), conforme o plano explicita que RealtimeMetricsPanel/ProgressiveDisclosureDashboard saem da Visão Geral até a Fase 9. `useAuth`/saudação removidos de `DashboardView.tsx` nesta fase (a lógica de saudação é explicitamente "movida para `GreetingBanner`" na etapa 33; manter useAuth sem uso aqui geraria variável morta).
- Vitest baseline (etapa 7): `src/hooks/dashboard/` já existe no worktree (só `index.ts` + `useDashboardStats.ts`, sem os hooks novos da Fase 5-8) e não tem testes ainda; `npx vitest run` nos 3 paths retorna "No test files found" (exit 1) — não é regressão, é ausência esperada pré-redesign.
- Retomada desta sessão (relançamento manual, geração 1): a sessão anterior morreu às 13:25 UTC rodando `pkill -f "4174"` — o padrão casou com o próprio argv do `claude -p` (o prompt continha "4174"), matando a própria sessão. Regra permanente adotada a partir de agora: nunca `pkill -f`/`killall` com padrão genérico (porta, vite, node, claude); só `kill <PID exato>` ou `fuser -k <porta>/tcp` depois de confirmar o PID via `/proc`. Etapas 10-17 (tokens, tailwind, ViewContainer, ViewRouter, DashboardView shell, OverviewSkeleton) já estavam editadas e corretas ao retomar — revisadas linha a linha contra o plano antes de continuar, nenhuma correção necessária nelas.
- Porta 4174 na retomada: **não estava livre** (ao contrário do que o prompt de retomada assumia — "preview 4174 MORTO"). PID 13008 (`node .../Zapp_Web_V2/node_modules/.bin/vite preview --port 4173`, do worktree **paralelo** de Contatos, iniciado 07:48 por um wrapper `bun run preview --port 4173` órfão) não conseguiu bindar 4173 (já ocupado desde ontem pelo PID 9539, `--strictPort`) e, por não ter `--strictPort`, o Vite incrementou silenciosamente para 4174 — ocupando a porta reservada ao dashboard. Confirmado via `/proc/net/tcp` (inode do socket) + varredura de `/proc/*/fd` por esse inode, já que `ss`/`fuser`/`lsof` não existem neste container. Ação: `kill 13008` (PID exato, processo isolado do worktree de Contatos — nenhum arquivo/git tocado lá, PID 9539 que serve a porta 4173 legítima não foi tocado). Novo `npx vite preview --port 4174 --strictPort` iniciado a partir deste worktree, PID real 33995 (depois 35821 após o rebuild da correção de ritmo vertical) confirmado via varredura de `/proc/*/cmdline` (não via `ps aux`/`$!`, que não refletem o processo real neste sandbox) e gravado em `/workspace/logs/redesign-dashboard-preview.pid`. O screenshot `01-after-1583.png` tirado às 10:24 (antes desta correção) capturou a build **errada** (do worktree de Contatos) — descartado e retirado às 10:38 contra o preview correto.
- CP1, ritmo vertical (medição inicial, antes do fix): tops medidos em +10 a +22px acima dos alvos da seção 2.1 — fora da tolerância ±6 do gate. Causa raiz 1 (corrigida): o `space-y-2.5` do container externo aplicava 10px de gap entre `dash-topbar` e `dash-header`; a referência mede os dois como adjacentes (faixa do topo termina em y=28+8=36, header começa em y=33+3=36 — gap ≈0). Fix: `dash-header` e `Tabs` movidos para um `div.space-y-2.5` interno separado, com `dash-topbar` fora dele — header agora começa exatamente no alvo (Δ0) e tabs também (Δ0), sem tocar em nenhum outro espaçamento. Causa raiz 2 (residual, não corrigida agora): `TabsList`/`TabsTrigger` nesta fase usam o componente shadcn **funcional, ainda não restilizado** (decisão da etapa 15, acima) — altura real 40px (`h-10 p-1`) contra os 32px (`h-8`) que a Fase 3 (etapa 29) vai aplicar ao pill redesenhado. Essa diferença de 8px acumula com pequenas variações de gap (9-11px medidos na referência vs 10px uniforme usado aqui) e explica os +10 a +12px residuais de `banner` em diante. Corrigir agora significaria adiantar o restyle de `TabsList` da Fase 3 fora de ordem (regra "fundação → shell → linhas de cima para baixo") — registrado como resíduo aceito, deve zerar sozinho ao fechar CP3.
- Etapa 20 (`DashboardTopBar`): o plano previa reabrir `AgentProfilePopover` (mesmo popover de perfil da sidebar) se ele existisse. Existe (`src/components/layout/AgentProfilePopover.tsx`), mas é **controlado por estado que só o `Sidebar` possui** (`collapsed`, `onStatusChange`, `onViewChange`, `onLogout`) — reaproveitá-lo no dashboard exigiria levantar esse estado para um contexto compartilhado ou duplicar a lógica de status/logout, fora do escopo de "diff mínimo" da Fase 2. Decisão: chip de usuário do dashboard usa o fallback previsto no próprio enunciado da etapa 20 — `navigateToView('settings')`.
- QA (não é código de produto): dois overlays globais pré-existentes colidem com screenshot/medição do dashboard — (1) modal "Bem-vindo" (tour, `fixed inset-0 z-[9999]`), que às vezes ainda está fechando/reabrindo entre as duas navegações da run e intercepta cliques; (2) `OnboardingChecklist` "Configure sua conta" (`absolute top-4 right-4`, só em `?view=dashboard`, `AppShell.tsx`) — **confirmado no `00-before-1583.png`** que já cobria os badges de gamificação do dashboard antigo, então não é regressão desta fase. `shot.mjs` ganhou um loop de dismiss (clique nativo via `evaluate`, até 5 tentativas) para os dois; sem isso o `02-after-1583.png` inicial saiu com o card do checklist tampando header/tabs por completo (retirado e substituído).
- QA, achado na Fase 3 (não é código de produto): o loop de dismiss do `shot.mjs` (5 tentativas de 300ms, checagem "cliquei ou não" antes de aceitar) tinha uma janela de corrida — o modal "Bem-vindo" pode montar com atraso (fetch de status de onboarding), depois do loop já ter concluído "nada a fazer" mas antes do `waitForTimeout(1200)` final que precede o screenshot. Resultado: `03-after-1583.png` (1ª captura) saiu com o overlay `bg-background/80 backdrop-blur-sm` ainda de pé — sutil demais para notar a olho nu num tema já escuro, mas deslocava toda amostra de cor pro fundo (ΔE~80+ onde deveria ser `--primary`). Fix: loop reescrito para (a) esperar 800ms antes de sequer checar, (b) verificar ausência real via contagem de `.fixed.inset-0.z-[9999]` (não só "não cliquei em nada"), (c) até 10 tentativas de 350ms, (d) log de aviso se não confirmar. `tabcheck.mjs` recebeu o mesmo fix (era a causa da falha intermitente no clique da aba "Sentimento" nos CP1/CP2). Screenshot e cliques de aba refeitos e confirmados limpos.
- Etapa 30 (pill deslizante): `motion.span` com `bg-primary -z-10` dentro do `TabsTrigger` só pinta visível se o trigger tiver stacking context próprio. `position:relative` sozinho não basta (`z-index` computado fica `auto`, sem contexto). Fix: `isolate` adicionado ao `TabsTrigger` (Tailwind `isolation:isolate`) — não estava no enunciado da etapa 29/30, é uma correção de implementação, registrada aqui porque não é óbvia e pode se repetir se algum outro `-z-*` for usado dentro de um elemento só com `relative`.
- CP3, `tabActiveWidth` (116px vs alvo≈139±12, faltando 11px pro piso 127): a etapa 29 prescreve exatamente `px-3` + `gap-1.5` + ícone `14px` — segui à risca — e o resultado natural com fonte Inter real é 116px. O alvo "139w" da seção 2.1 vem de medir o PNG de referência (mockup gerado por IA, não DOM real), cujas métricas de fonte não são necessariamente as do Inter renderizado no browser. Não ajustado: mudar padding/gap pra "bater o número" contrariaria a instrução mais específica (etapa 29) em favor de uma medida aproximada da imagem — registrado como resíduo, não regressão.

## Iterações do loop visual (máx 3 por fase)
- CP1 (1/3): ritmo vertical dos placeholders fora da tolerância (+10 a +22px). Fix aplicado: `dash-topbar` retirado do `space-y-2.5` que o unia ao `dash-header` (gap 10→0, conforme a referência mede os dois blocos adjacentes). Resultado: header/tabs Δ0; banner em diante ficou com resíduo de +10-12px, causa raiz identificada (TabsList funcional 40px vs 32px alvo da Fase 3) e registrado em Divergências — não é regressão, deve resolver ao fechar CP3. Não precisou de 2ª/3ª iteração.
- CP2 (1/3): chip de período com `w-[167px]` fixo não respeitava "< md → w-full" da etapa 25. Fix: `w-full md:w-[167px]` (mobile-first); confirmado 332px@390 e 642px@700 (full width do container), 167px inalterado em 1583/1920. Não precisou de 2ª/3ª iteração.
- CP3 (1/3): pill deslizante (`motion.span layoutId` + `bg-primary -z-10`, exatamente como a etapa 30 especifica) renderizava **invisível** — amostra de cor batia com o fundo da página, não com `--primary`. Causa: `-z-10` sem stacking context próprio no `TabsTrigger` (`position:relative` sozinho não cria um; `z-index` computado ficava `auto`) escapava para fora do componente e pintava atrás de tudo. Fix: `isolate` adicionado ao className do trigger (cria stacking context sem precisar de z-index explícito, mantendo o `-z-10` da etapa 30 intacto). Confirmado por scan de pixels (nenhum azul antes, bloco sólido `rgb(20,110,255)` depois) e `colors.mjs` (ΔE 0.00). Não precisou de 2ª/3ª iteração.

## Pendências / resíduos (honestos)
- Shell (fundo/sidebar/primary) = tokens da branch de Contatos; referência do dashboard é mais neutra (ΔE 8.2 no fundo, 7.6 na sidebar) — um app, uma paleta
- Sidebar 234px (referência: 193px) — decisão do shell compartilhado
- KPI 4: tile verde + barras violeta (como na referência)
- "Capacidade máxima" omitida (sem fonte); deltas de Conversas Abertas/Não Lidas omitidos; barras de Não Lidas/Atendentes omitidas
- Frase motivacional: copy de UI (7 frases), não dado
- Emojis da referência → ícones lucide; 2 sinos → 1; tooltip do gráfico só no hover; fotos = avatar_url/iniciais
- Ícone da tab "Visão Geral" mantido (TrendingUp)
