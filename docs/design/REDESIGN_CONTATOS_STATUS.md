# Redesign Contatos — Navy Premium — STATUS
Branch: redesign/contatos-navy-v2 · Base: 6c917044 · Preview Vercel: https://zappwebv2-git-redesign-contatos-navy-v2-juca1.vercel.app (atrás de Vercel SSO — ver divergências) · QA automatizado: `npm run preview` local (porta 4173) contra Supabase Cloud oficial
Repo path (container): /workspace/repos/Zapp_Web_V2 · Playwright: ok (chromium + --with-deps instalado, sem bloqueio) · QA user: ok (login validado via /auth/v1/token)

## CP0 Ambiente        [x] sha=61d35bb955cbe999b0217477a202335efc52bc0e · before=/workspace/qa/out/00-before.png (produção, https://zapp-web-v2.vercel.app/?view=contacts, 1672x941) · gates baseline: typecheck=OK(0 erros) lint-ratchet=OK(baseline=1223 atual=1216 novas=0) tc-ratchet=OK(baseline=0 atual=0) implicit=OK(0, após fix) vitest=OK(24/24 em src/components/contacts)
## CP1 Paleta/Fonte    [x] sha=ffbb2d12d7ce19825adbb071c5a85c730defa1a7 · shot=/workspace/qa/out/01-after.png (preview local — ver divergência Vercel SSO) · colors (getComputedStyle :root): background=ΔE0.86 card=ΔE0.78 sidebar=ΔE0.91 input=ΔE0.78 primary=ΔE1.60 success=ΔE1.18 · fontInter=true
## CP2 Shell           [x] sha=0dbc4a57d428e6ff279771b76f8fbbd26b6ee127 · shot=/workspace/qa/out/02-after.png · sidebar=234±4 OK navItem=44±2 OK logo=44 (w-11 h-11, exato por classe) · dashboard=/workspace/qa/out/02-dashboard.png inbox=/workspace/qa/out/02-inbox.png ambos scrollW=innerW=1672 (sem overflow)
## CP3 Header          [x] sha=b05fc7addeb49b746e2a0860af8e011d2aae5df0 · shot=/workspace/qa/out/03-after.png · headerBtn=48 crmBtn=48 title=38 topRightSearch=220x36 · sidebar=234 navItemActive=44 (regressão-check OK) · fontInter=true scrollW=innerW=1672 · crmBtn bg=rgba(20,110,255,.2) (token primary/20, receita da etapa 35) · novoContatoBtn bg=rgb(0,214,100) (token success, alvo #00d464, ΔE≈0.3) · console errors=0
## CP4 KPIs            [x] sha=33be3a0af4ccb812bd41cccd56922c7394550731 · shot=/workspace/qa/out/04-after.png · kpi=[108,108,108,108] tile=60 · total==todos: sim (ambos vêm de contactCountByType['all']=1.516) · lid_legacy filtro: não (search_contacts/contacts_count_by_type não filtram is_lid_legacy — grep em supabase/migrations confirma; hook usa o mesmo critério para não divergir do Total/badge das tabs) · tiles ΔE: blue rgb(1,47,121) vs alvo rgb(1,48,123) ΔE≈0.6 · green rgb(5,66,54)=alvo exato ΔE=0 · purple rgb(37,32,100) vs alvo rgb(37,33,101) ΔE≈0.3 · yellow rgb(61,54,26) vs alvo rgb(59,53,24) ΔE≈0.8 · vitest 38/38 (24 pré-existentes de contacts + 6 novos ContactStatsCards.test.tsx + 8 novos useContactsKpi.test.ts)
## CP5 Tabs            [x] sha=65f19c4f89a3a088dfcac25613c641ac047488b8 · shot=/workspace/qa/out/05-after.png · tabBar=52 tabActive=40 todosW=161 (alvo 152±12, OK) · pill layoutId anima na troca · badge ativo bg-primary, inativo bg-muted · vitest 30/30
## CP6 Toolbar         [x] sha=8988dbc6d657f5c364b213e385445956a1ca5cc9 · shot=/workspace/qa/out/06-after.png · search=44 sort=44 filtros=44 presets=44 seg=44 colunas=44 · uma linha: sim (offsetTop=390 idêntico em todos, medido via /workspace/qa/toolbar-check.mjs) · Agrupamento movido para dentro de Colunas (LayoutList standalone removido) · segmentado com pill layoutId="contacts-view-pill" · vitest 30/30
## CP7 Resultados      [x] sha=83f1ba9e55ed84727ae0e5484ce4a3e840ca5bd8 · shot=/workspace/qa/out/07-after.png · pager=36 (alvo 36±2, exato) · checkbox=18px rounded-[5px] · vitest 30/30 · espaçamento: header→toolbar em space-y-4(16px), toolbar→resultados→grid em space-y-3(12px) — mais compacto que o alvo da seção 2.4 (18±4/20±4), seguido literalmente conforme a instrução da etapa 68 ("space-y-4 acima, space-y-3 abaixo"), não a medida em px do mockup; resíduo honesto, não é gate do CP7
## CP8 Cards           [x] sha=f6abbdf5ce948ea33d607f88ccf6bd8f5cda7b90 · shot=/workspace/qa/out/08-after.png · compare=pulado (sem PNG de referência no repo, ver etapa 6) · card=178 (min-h 164 ok) avatar=64 footerBtn=36 colunas=4 gap≈12 (deltaX 344-345, cardW≈332) · badge Cliente: bg=rgba(5,35,87,.9) border=rgba(20,110,255,.6) texto=rgb(153,192,255) ΔE(fundo vs #042255)=1.85 contraste texto/fundo=8.36:1 (alvo ≥4.5) · vitest 30/30 · achado importante registrado nas divergências (Tailwind não compila `/NN` sobre valores arbitrários)
## CP9 Secundárias     [x] sha=661219c0a647327a2332c6cacbeedb3b73002d27 · shots=/workspace/qa/out/09-list.png,09-table.png,09-detail.png · Lista h-16 avatar 40 badge pill · Tabela thead bg-muted/40 linhas h-14 badge pill · Detalhe backdrop bg-background/60 blur, Esc fecha (mantido) · nenhum elemento cinza-violeta remanescente nos 3 screenshots (inspeção visual) · vitest 30/30
## CP10 Motion         [x] sha=b2779db0d1e759699200d70f6e3231fabd137057 · reduced-motion: durations≈0 (1e-05s, confirmado via Playwright emulateMedia reduced-motion:reduce em /workspace/qa/reduced-motion-check.mjs — cards, sparklines KPI, pills das tabs) · mecanismo: `<MotionConfig reducedMotion="user">` envolvendo `ContactsView` (framer-motion 12), em vez de checar `useReducedMotion()` manualmente em cada ponto — cobre globalmente sparkline/count-up/pills/cards/dialogs sem precisar tocar cada componente · AnimatePresence mode="wait" na troca de viewMode (120ms) · cards animam entrada só no 1º mount (flag via useState+rAF, não useRef — o novo lint react-hooks/refs proíbe ler ref.current durante render) · ContactContentArea.tsx grid: gap-3 mantido · bundle: ContactsView-*.js = 226.8KB raw / 60.2KB gzip (medido nesta build); Δ vs. antes do redesign não medido — teria exigido rebuild do HEAD pré-fase-3 (df5321b2), não fiz por risco/custo; `node scripts/ci/bundle-budget.mjs` (bundle inicial/entry) passa: 339.5KB de 350KB budget · animate-pulse: só em skeletons (grep confirmado) · vitest 30/30
## CP11 Fidelidade     [x] sha=dab7fe465e3d08889093abe00d486710ae555145 · final=/workspace/qa/out/11-final.png · geometria: 17/17 OK (sidebar234 navItem44 title38 headerBtn48 kpiCard108×4 kpiTile60 tabBar52 tabActive40 tabTodosW161 toolbar44 topRightSearch220x36 pager36 card178 avatar64 footerBtn36 kpiTotal==tabBadge fontInter) · cores: 6/6 OK via getComputedStyle(:root) (ΔE 0.78–1.60, todas ≤6) + badge Cliente ΔE1.85 (CP8) · E.4 pulado (sem PNG de referência, etapa 6) · light=/workspace/qa/out/11-light.png OK (scrollWidth=innerWidth=1672) · mobile=/workspace/qa/out/11-mobile.png OK (scrollWidth=innerWidth=390) · vitest 30/30
## CP12 Entrega        [x] PR=https://github.com/adm01-debug/Zapp_Web_V2/pull/261 (mergeado — merged=true, merged_at=2026-09-07T14:47:26Z) · CI=amarelo, aceito (ver nota, falha pré-existente confirmada também pós-merge) · merge=5997d4675308499e8dab85d15f6b93f002b3043b (confirmado ancestor de origin/main via `git merge-base --is-ancestor`) · prod=/workspace/qa/out/12-prod.png · colors prod ok
### CI do PR #261 (verificado via API, todos os checks concluídos)
OK: Contrato DB offline · CodeQL (javascript-typescript) · Vercel Preview Comments (success) · Vercel Agent Review (neutral) · cubic AI code reviewer (neutral) · Mermaid/Supabase Preview/Build/Security Audit/Unit Tests/Generate Audit Report (skipped, não aplicável ao diff)
**FALHA: 🔍 Lint & TypeCheck** — causa raiz isolada nos logs do job (`supabase/functions/*` × `supabase/deployment-manifest.json` dessincronizados: "Deployment manifest is stale... Run generate-manifest.mjs"). **Confirmado que essa mesma falha já existe hoje no HEAD de `main`** (`Lint & TypeCheck: failure` e `Contrato DB vivo: failure` também em `main`, checado via `GET /commits/main/check-runs`) — ou seja, é um problema pré-existente no repositório, não uma regressão introduzida por este PR. Esta branch nunca tocou `supabase/` (confirmado: `git diff main...HEAD --stat` não lista nenhum arquivo em `supabase/`).

### Etapa 98/99 — merge (decisão humana, executada fora desta sessão)
- PR #261 foi mergeado por `adm01-debug` em `2026-09-07T14:47:26Z` (`merged:true`, `merge_commit_sha:5997d4675308499e8dab85d15f6b93f002b3043b`, verificado via `GET /repos/adm01-debug/Zapp_Web_V2/pulls/261`). A decisão de aceitar a falha pré-existente de CI (registrada como pendente na sessão anterior) foi tomada fora desta sessão — encontrada já concluída ao retomar o trabalho.
- `git fetch origin main` confirma `5997d467` como HEAD de `origin/main` (`5a5896ab..5997d467 main -> origin/main`), local `main` sincronizável via fast-forward.
- Checks pós-merge no commit `5997d467` (`GET /commits/5997d467.../check-runs`): `Contrato DB vivo: failure` e `Lint & TypeCheck: failure` continuam presentes em `main` após o merge — confirma que é o mesmo problema pré-existente (manifest de deploy do Supabase dessincronizado), não algo introduzido por este PR. `Supabase Preview: success`, `CodeQL: success`, `Contrato DB offline: success`.
- Corpo do PR confirmado com a seção "Resumo" da entrega (7491 caracteres, `## Resumo` + mudanças por fase), conforme DoD da etapa 98.

### Etapa 100 — verificação de deploy de produção
- `GET /commits/5997d467.../status`: `state=success`, contexto `Vercel` = `success` ("Deployment has completed"), confirmando deploy do SHA mergeado em produção.
- QA automatizado contra produção real (`node /workspace/qa/prod-final.mjs`, `https://zapp-web-v2.vercel.app/?view=contacts`, skin limpo, sidebar expandida): screenshot `/workspace/qa/out/12-prod.png` (1672×941), 0 erros de console.
- E.3 (cores) em produção, `getComputedStyle(:root)`: background ΔE=0.86 · card ΔE=0.78 · sidebarBg ΔE=0.91 · input ΔE=0.78 · primary ΔE=1.60 · success ΔE=1.18 — todas ≤6 (fundos) / ≤8 (accent), **RESULT: ALL OK**.
- Inspeção visual do screenshot: paleta navy, header com CRM 360°/Sincronizar/Novo Contato, 4 KPI cards, tabs pill "Todos 1.516", grid de cards Cliente — consistente com o design entregue nos checkpoints CP0–CP11, dados reais (1.516 contatos, mesma base do CP0).
### Evidência etapa 96 (gates técnicos)
- `npm run typecheck`: 0 erros
- `node scripts/ci/lint-ratchet.mjs`: baseline=1223 atual=1216 novas=0
- `node scripts/ci/typecheck-ratchet.mjs`: baseline=0 atual=0 novas=0
- `node scripts/ci/implicit-any-ratchet.mjs`: 0 (baseline 0)
- `bun run lint` (raw, sem ratchet): 841 erros/375 warnings pré-existentes no repo inteiro (bate com o baseline=1223 do ratchet) — nenhum arquivo tocado nesta redesign está nessa lista
- `bunx vitest run` (suíte inteira): 2801 passed, 35 todo, **2 failed** — `GroupsView.test.tsx` (timeout em `waitFor`) e `MediaLibraryAdmin.test.tsx` ("100 items without crash", timeout 15s) — **nenhum dos dois é de `src/components/contacts`**; reexecutados isoladamente (`vitest run <esses 2 arquivos>`) e passam 100% (292/292) — flakiness de timeout sob carga da suíte inteira (2838 testes em paralelo), não regressão desta branch
- `npm run build`: OK, 12-15s
- `node scripts/ci/bundle-budget.mjs`: JS inicial 339.5KB/350KB budget, CSS 33KB/80KB — OK

### Evidência etapa 97 (QA funcional, `/workspace/qa/func.mjs`, contra preview local)
20/20 checks OK, 0 console errors: busca "a" retorna resultados · limpar busca · tab Cliente filtra · sort "Mais recentes" · Filtros abre/fecha · Filtros Salvos abre · Selecionar todos → BulkActionsBar · Lista/Tabela/Pipeline/Mapa/Analytics renderizam sem erro de console · volta pra Cards · Colunas muda para 3 · Agrupar por empresa · paginação pág.2 e volta · abrir detalhe + Esc fecha · Novo Contato abre form (sem submeter) · CRM 360° abre · Sincronizar dispara.

## Divergências plano × código (o que o plano dizia vs. o que existia)
- Etapa 3: `graphify-out/GRAPH_REPORT.md` estava desatualizado no início da execução (indexado em `504fa12d`, HEAD real `6c917044`). Rodado `graphify update . --force` — grafo agora bate com HEAD (`6c917044`). `graphify path "ContactsView.tsx" "tokens.css"` (direcionado e não-direcionado) não encontra caminho: o grafo de imports/AST não enxerga o consumo de `tokens.css` porque ele é feito via classes Tailwind (`bg-card`, `border-border` etc.) e não via `import` — confirma o diagnóstico da seção 0.1 do plano (troca de componente sem tocar token não muda nada visível), mas o grafo em si não "prova" essa ligação por não rastrear `var(--x)`/classes Tailwind.
- Etapa 6: `docs/design/contatos-reference.png` não existe no repo. **E.4 (composite/heatmap) fica pulado** em todos os checkpoints; E.2 (geometria) e E.3 (cores) são os gates obrigatórios usados.
- Etapa 9: `package.json.engines.node` exige `>=24`, mas o Node do sistema é `v20.20.2`. `bun` (`1.3.14`, satisfaz `>=1.3`) está disponível e foi usado para `install`/scripts em vez de `npm ci`, conforme a alternativa já prevista na própria etapa 9 ("`npm ci` (ou `bun install --frozen-lockfile`)"). `node_modules` já estava presente e íntegro (833M, 560 pacotes); `bun install --frozen-lockfile` só completou o que faltava.
- Etapa 9 (baseline "tudo verde antes de mexer"): `npm run implicit-any-check` falhou de saída com 2 erros pré-existentes em `src/hooks/communication/useSpeechToText.ts` (TS7006, parâmetros `event` dos handlers `onresult`/`onerror` da Web Speech API), arquivo **fora do escopo do módulo Contatos** e sem relação com o redesign. `scripts/ci/implicit-any-baseline.json` registra `baseline: 0`, ou seja, a baseline documentada já estava desatualizada em `main` antes desta execução. Decisão (regra 15, código real vence + necessidade de baseline verde para todos os 12 gates futuros): corrigido com diff cirúrgico de 2 linhas (anotação explícita `event: any` + `eslint-disable-next-line @typescript-eslint/no-explicit-any`, mesmo padrão já usado no arquivo para `SpeechRecognitionInstance`), commit isolado `61d35bb9` **antes** de qualquer commit de fase do redesign. `typecheck`, `lint-ratchet` e `tc-ratchet` continuaram OK antes e depois do fix.
- **Etapa 22 (todas as fases): preview do Vercel por branch está atrás de Vercel Deployment Protection (SSO)** — `https://zappwebv2-git-redesign-contatos-navy-v2-juca1.vercel.app` responde `302` para `vercel.com/sso-api` mesmo sem interação, inacessível a Playwright headless (não há `VERCEL_AUTOMATION_BYPASS_SECRET` em `/workspace/.secrets`, e o MCP `vercel` exige OAuth interativo indisponível nesta sessão). **Produção não tem essa proteção** (confirmado no CP0). Decisão: para os gates E.1/E.2/E.3 de cada fase, uso `npm run build && npm run preview` (vite preview, porta 4173) servido localmente no container, apontando para o **mesmo banco Supabase Cloud oficial** (`tnnnlkbymytvtqngbbqh`, igual a `VITE_SUPABASE_URL` de `.env.production`) — dados reais idênticos aos da produção, só a origem do bundle estático é local em vez do edge da Vercel. O push da branch e o preview do Vercel continuam acontecendo normalmente a cada fase (para o Joaquim revisar via browser autenticado); só a verificação automatizada usa o preview local. A checagem final de produção (etapa 100) usa a URL pública real, sem esse desvio.
- **Etapa 21/CP1 — achado de arquitetura**: `--primary` (e `secondary`, `accent`, `ring`, `sidebar-primary`, `sidebar-accent`, `chat-bubble-sent`, `status-open`, `chart-1`) **não são lidos do CSS estático em runtime** — `ThemeInitializer.tsx` e `useThemePreset.ts` os reescrevem via `root.style.setProperty` a partir dos parâmetros `h/s/l/gh/sh/ss/sl` do preset ativo (`CSS_VARS_TO_APPLY`, `presets.ts:106`), mascarando qualquer valor definido em `tokens.css .dark`. Isso não está documentado no plano (Apêndice A assume que editar `tokens.css` basta). Como o preset ativo por padrão é `'corporate'` (h:221, não h:217 da paleta navy), a amostra de `primary` media ΔE=6.08 (acima do gate ≤6) mesmo com `tokens.css` correto. Corrigido em `ffbb2d12` sobrescrevendo campos do `corporate.dark` pós-`buildPreset` (mesmo padrão já usado pelo preset `'diversity'` no fim do arquivo) — só o modo escuro do preset corporate muda; light mode e os demais 8 presets (purpure, emerald, sunset, rose, minimal, ocean, amber, cyber) ficam bit-a-bit idênticos a antes. ΔE de primary caiu para 1.60.
- **Etapa 19 — armadilha do lint-ratchet**: a primeira tentativa de gate de versão do skin (`if (parsed.v !== STORAGE_VERSION) throw ...` inserido *antes* de `setActivePreset(presetId)` em `useThemePreset.ts`) deslocou 3 linhas acima de uma violação legada (`react-hooks/set-state-in-effect`, linha 70 no baseline), fazendo o `lint-ratchet` reportá-la como nova (a mensagem do ESLint embute números de linha do trecho, então o `contextHash`/`message` deixam de bater mesmo com o texto das linhas vizinhas idêntico). Corrigido movendo a lógica de versão para **depois** do bloco legado (dentro da condição de re-save já existente) e combinando os imports/campos novos na mesma linha de campos existentes, zerando o deslocamento — `lint-ratchet` voltou a `novas=0`. Confirma na prática a regra 10 do CLAUDE.md do projeto.
- **Etapa 6/29 — Apêndice E.1 usa a chave de localStorage errada**: o script `shot.mjs` do plano faz `localStorage.removeItem('sidebar-collapsed')`, mas o hook real é `useSidebarCollapse.ts` com chave `zapp-sidebar-collapsed` (default = colapsada quando ausente). Isso fazia os screenshots de QA capturarem a sidebar **colapsada** (rail de ~62px), divergindo da geometria medida no plano (seção 2.3 assume sidebar expandida 234px). Corrigido em `/workspace/qa/shot.mjs` e `measure.mjs` para `localStorage.setItem('zapp-sidebar-collapsed', 'false')`. Confirmado que `--sidebar-w: 234px` (token da Fase 1) já refletia corretamente assim que a sidebar é expandida — nenhum ajuste de largura foi necessário na Fase 2, só nos itens internos.
- **Etapa 93/95 — achado real: pill ativa das tabs e do segmentado Cards/Lista/Tabela nunca teve fundo colorido visível (bug das Fases 5 e 6, achado só agora no QA de luz)**: o `motion.span` com `layoutId` e `-z-10` (`bg-primary -z-10` posicionado `absolute inset-0` dentro de um `<button>`/`TabsTrigger` só com `position: relative`, sem `z-index` próprio) não cria stacking context isolado — por spec de CSS, um filho com z-index negativo sem stacking context próprio do pai pode acabar renderizando atrás do `bg-card` do **avô** (o container do segmentado/tabs), não só atrás dos irmãos do próprio botão. O texto "Cards"/"Todos" continuava legível em modo escuro (`text-white` sobre fundo escuro, por coincidência) — por isso passou despercebido nos CP5/CP6 — mas em modo claro ficou **texto branco sobre fundo claro, invisível**, o que só apareceu ao gerar `11-light.png` para este checkpoint. Diagnosticado isolando o elemento (`page.locator(...).screenshot()` retornou PNG em branco) e comparando com o DOM (`getBoundingClientRect` válido, mas nada pintado). Corrigido adicionando `isolate` (CSS `isolation: isolate`) nos triggers de `ContactTypeTabs.tsx` e nos botões de `ContactViewSwitcher.tsx`, forçando cada botão a criar seu próprio stacking context — a pill volta a ficar contida e visível nos dois temas. Sem esse achado, o "Cards" ativo teria ido para produção sem nenhuma pill visível há duas fases.
- **Etapa 95 — mobile 390px: cabeçalho de Contatos quebrava/sobrepunha (regressão real da Fase 3)**: `PageHeader` `variant="plain"` fixava a linha do breadcrumb+`topRight` em `h-14` sem `flex-wrap`, e `ContactsTopActions` usava um input de busca fixo em `w-[220px]` — em 390px de viewport isso sobrepunha o breadcrumb ("Início › Gestão › Contatos") com a barra de busca/sino/avatar. Os 3 botões do header (`CRM 360°`/`Sincronizar`/`Novo Contato`) também não quebravam linha e estouravam a tela. E o valor de KPI (34px) + badge de delta cortavam nas colunas estreitas do grid mobile (`grid-cols-2`). O gate literal do plano (`scrollWidth <= innerWidth`) **passava mesmo assim** (o navegador não criava scrollbar — os elementos só ficavam sobrepostos/cortados via layout, não via overflow com scroll), o que teria deixado a quebra visual passar por um "OK" técnico enganoso. Corrigido: `flex-wrap` na linha do header (`sm:h-14` em vez de `h-14` fixo), busca do topo vira ícone-only abaixo de `sm`, botões do header com `flex-wrap`, valor do KPI responsivo (`text-[26px] sm:text-[34px]`) e sparkline oculta abaixo de `sm`. Confirmado com screenshot novo (`11-mobile.png`) — sem sobreposição, sem corte, `scrollWidth=innerWidth=390`.
- **Etapa 70 — achado de arquitetura: Tailwind aqui não compila modificador de opacidade sobre valores arbitrários**: a receita original do plano (`border-[hsl(H_S%_L%)]/60 ... bg-[hsl(H_S%_L%)]/15`) — e o código **já existente antes desta fase** nos mesmos moldes (`bg-[hsl(217_91%_60%)]/8` etc.) — nunca gerou CSS. Confirmado inspecionando `dist/assets/index-*.css` pós-build: zero ocorrências de qualquer classe `.{border,bg,text}-\[hsl\(...\)\]\/[0-9]+` no bundle, para qualquer matiz. Também tentei embutir o alfa dentro do próprio `hsl(..._/_0.NN)` (outra forma comum de contornar o modificador) — também não compila neste Tailwind 3.4.19/config do projeto. O que **funciona** e foi confirmado no bundle: `rgba(r,g,b,a)` literal com vírgulas — o JIT converte para hex+alfa (`bg-[rgba(20,110,255,0.15)]` → `background-color:#146eff26`). Reescrevi as 9 entradas de `badgeClass` em `contactTypeConfig.tsx` usando `rgba()` (convertido manualmente de HSL→RGB) para border/bg, mantendo `text-[hsl(...)]` sem alfa (esse já funcionava). Isso corrige um bug latente que **já existia em produção antes do redesign**: nenhum badge de contato jamais teve fundo ou borda coloridos, só o texto — ninguém tinha notado porque não há teste visual automatizado no CI.
- **Etapa 70 — badge Cliente precisou de uma cor sólida, não da receita genérica de 15%**: a composição `rgba(primary,0.15)` sobre o fundo do card (`rgb(13,23,38)`) nunca consegue chegar perto de `#042255` (alvo da seção 2.1) porque o canal R do fundo do card (13) já é maior que o R do alvo (4) — nenhum alfa entre 0 e 1 resolve algebricamente (a matemática está no ledger, não no código). Troquei o fundo do badge Cliente para `rgba(5,35,87,0.9)` (a própria cor `hsl(218,90%,18%)` da tabela 2.1, quase opaca) — `ΔE=1.85` (alvo ≤8), contraste texto/fundo `8.36:1` (alvo ≥4.5). Os outros 8 tipos continuam na receita genérica de 15% (não gated por ΔE numérico no plano).
- **Etapa 52 — as 10 abas (Todos + 9 tipos) não cabem em uma linha sem scroll mesmo em viewport 1672px**: medido via `/workspace/qa/tabs-check.mjs` (`document.querySelector('[role="tablist"]')`): `scrollWidth=1460` vs `clientWidth=1364` — a área de conteúdo real em 1672px de viewport já é só ~1366px (1672 − sidebar 234 − gutter 2×36, estabelecido nas Fases 1/2), e os labels mais longos ("Prestador de Serviço", "Sicoob Gifts", "Transportadora") nos tamanhos exigidos pelo plano (15px/500, `px-4`, `gap-1`, ícone 18px, badge 24px) somam ~96px a mais do que o espaço disponível. Não reduzi fonte/padding abaixo do que o plano especifica com tolerância explícita (isso quebraria outro gate). Mantido `overflow-x-auto` + `snap-x` (já previsto na etapa 52 para telas menores) também em 1672 — rolagem horizontal suave, sem quebra de linha, sem scrollbar visível (barra fina). DoD "em 1672 cabem todos sem scroll" não bate 100% com a geometria real de sidebar+gutter já fixada; registrado para decisão de produto (a alternativa seria abreviar rótulos ou reduzir para <15px, ambos fora do que o plano autoriza sem confirmação).
- **Etapa 40 — `is_lid_legacy` não é filtrado por nenhuma das duas RPCs**: `grep -rn "is_lid_legacy" supabase/migrations/*.sql` só retorna a migration que criou a coluna (`20260906000001_e31_contacts_is_lid_legacy.sql`); nem `search_contacts` nem `contacts_count_by_type` a referenciam. Decisão: `useContactsKpi` também não filtra (`filterLidLegacy=false` no call site de `ContactStatsCards`), para que `deltaTotalPct`/`empresasDistinct`/etc. sejam calculados sobre a mesma população que gera `contactCountByType['all']` (usado nas tabs e no card Total) — evita a KPI Total divergir do badge "Todos".
- **Etapa 41/45 — `deltaTotalPct` explode para um número absurdo com o dataset real desta base**: verificado via REST direto (`/workspace/qa/verify-data.mjs`, login com o usuário QA, `Prefer: count=exact`) que **as 1.516 linhas de `contacts` têm `created_at` dentro dos últimos 30 dias** (mais antiga: `2026-08-27`, "hoje" é `2026-09-07`) e **zero linhas** na janela de 30-60 dias atrás. A fórmula do Apêndice C (`pct(total, max(total-novos30,1))`) usa `total-novos30` como proxy do "total há 30 dias"; com quase toda a base recente esse denominador vira `max(0,1)=1`, e o resultado é uma porcentagem de dezenas de milhares (`+99900%` renderizado). Confirmado que **não é bug de implementação** — é a fórmula especificada no plano se comportando exatamente como escrita, só que essa base real (aparentemente criada/migrada em bloco em `2026-08-27`, sem histórico anterior) é um caso degenerado para essa fórmula. Não alterei a fórmula por conta própria (não estava autorizado a redesenhar a métrica); mantive fiel ao Apêndice C e registro aqui para decisão de produto — a mesma degenerescência devia acontecer com `deltaNovosPct` sempre que `novosPrev30=0` e `novos30>0` (vira sempre exatamente +100%, que é o comportamento visto no card "Novos (30 dias)").
- **Etapa 33 — não existe "view de notificações" no código real**: `grep -rn "notification" src/pages/ViewRouter.tsx` não retorna nada; `VIEW_MAP` do `ViewRouter.tsx` não tem entrada `notifications`, só existe uma aba "Notificações" dentro de `SettingsView.tsx` (`Tabs defaultValue="schedule"`, não controlado, sem deep-link por query param). Além disso `ContactsView` é montado por `ViewRouter` sem nenhuma prop (`<ViewComponent />` em `VIEW_MAP`, linha ~127), então não há `onNavigateTo` disponível dentro da árvore de Contatos sem tocar `ViewRouter`/`Index.tsx` — proibido pela seção 3.3 do plano. Decisão (regra 15): o sino de `ContactsTopActions.tsx` dispara `document.dispatchEvent(new CustomEvent('open-command-palette'))`, mecanismo real já existente e escutado por `GlobalKeyboardProvider.tsx` (mesmo Cmd+K), de onde o usuário navega para Configurações. Não é uma central de notificações dedicada — é o mecanismo real mais próximo sem inventar componente novo nem tocar arquivo proibido. Registrado como pendência honesta.

## Iterações do loop visual (máx 3 por fase)
- CP1: 1ª iteração — tokens.css + presets.ts (neutros) editados; amostra de `primary` deu ΔE=6.08 (falhou o gate ≤6). 2ª iteração — identificada causa raiz (override runtime via `CSS_VARS_TO_APPLY`), corrigido `corporate.dark` pós-build; `primary` ΔE=1.60. Gate fechado na 2ª iteração.

## Validação da armadilha do skin (etapa 19, `/workspace/qa/skin-trap-check.mjs`)
- Injetado skin legado (`theme-custom-colors` sem campo `v`, `cssVarsCache` com `background: 240 8% 6%`, `primary: 221 83% 53%`) via `addInitScript` antes do boot.
- Inline style do `<html>` logo após o script de boot (antes do React montar): `--background: 216 58% 8%` (navy) — o guard `c.v === 2` do `index.html` rejeitou o cache legado corretamente, sem flash de cinza-violeta.
- Após o React montar: `getComputedStyle` confirma `--background: 216 58% 8%`, `--primary: 217 100% 54%`; `localStorage['theme-custom-colors']` reescrito com `"v":2` e `cssVarsCache` navy fresco.
- Resultado: **OK** — skin antigo descartado/atualizado, sem intervenção manual do usuário.

## Pendências / resíduos (honestos)
- Fonte Inter ≠ fonte da referência (larguras ±3%)
- Fotos: avatar_url real ou iniciais (referência usa rostos gerados)
- Séries e deltas: valores reais (referência tem números ilustrativos)
- Sino do header de Contatos não abre uma central de notificações real (não existe uma no app) — abre a paleta de comandos global (Cmd+K), ver divergência da etapa 33
- KPI "Total de Contatos" mostra um delta percentual absurdo (dezenas de milhares de %) nesta base real porque 100% dos 1.516 contatos foram criados nos últimos 30 dias (sem histórico anterior) — fórmula fiel ao Apêndice C do plano, decisão de produto pendente (ver divergência da etapa 41/45)
- As 10 abas de tipo rolam horizontalmente mesmo em 1672px (não cabem todas em uma linha nos tamanhos especificados) — ver divergência da etapa 52
- CI do PR #261 nunca ficou verde (falha pré-existente pré-merge/pós-merge em `main`: `Lint & TypeCheck`/`Contrato DB vivo`, manifest de deploy Supabase dessincronizado) — mergeado mesmo assim por decisão humana (fora desta sessão); corrigir `supabase/deployment-manifest.json` está fora do escopo desta redesign

## Status final
**concluído** — CP0–CP12 fechados com evidência (17/17 geometria, 6/6 cores ≤ΔE6 + badge Cliente ΔE1.85, QA funcional 20/20, gates técnicos 8/8, PR #261 mergeado em `main` (sha `5997d4675308499e8dab85d15f6b93f002b3043b`), produção verificada (`12-prod.png`, 6/6 cores OK, 0 console errors). Pendências honestas acima (nenhuma bloqueia entrega; são decisões de produto ou limitações de dados/fonte já registradas).
