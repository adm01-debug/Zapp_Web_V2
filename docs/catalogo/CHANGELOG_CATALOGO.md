# Changelog — módulo Catálogo

Formato: `E<nn> · <título> · <commit/PR> · <data>`

## Fase 0 — Saneamento & base

- E02 · Branch `feat/catalog-f0-base`, plano + prints em `docs/catalogo/`, `scripts/catalog/validate-plan.mjs`, `catalog` em `COMPACT_GUTTER_VIEWS`, link em `docs/README.md` · 2026-09-11
- E01 · Diagnóstico de gates e disco (ver `ESTADO_INICIAL.md`) · 2026-09-11

## Fase 2 — Backend

- E21 · Edge `promogifts-catalog` v2: `PRODUCT_FIELDS` completos (images, color_swatches, materials, tags, flags is_featured/is_new/is_bestseller/is_on_sale/is_closeout + expirações, engraving_*, main_category_id, order_count, view_count, created_at/updated_at/last_sync_at, primary_image_fallback_url) e `compact=true` opcional para o card; tipos em `useExternalCatalog.ts`; manifest regenerado. Colunas validadas contra o DB externo (PO-13153: 6 imagens, 1 swatch, LASER). Deploy só via `deploy-functions.yml` a partir de main (PAT do VPS retorna 403) · 2026-09-11

**Deploy E21 executado** (run [34661069952](https://github.com/adm01-debug/Zapp_Web_V2/actions/runs/34661069952)): `promogifts-catalog` deployado com sucesso em produção (digest `sha256:c52405…`). O run aparece como falho por drift **pré-existente e não relacionado**: `talkx-report` consta no manifesto mas nunca foi deployado (módulo Talk X, pendente de `RESEND_API_KEY`) — fora do escopo do Catálogo.

- E03 · Remoção do legado órfão: `ProductManagement.tsx`, `ProductCatalog.tsx`, `ProductCard.tsx`, `ProductForm.tsx`, `ShoppingCart.tsx`, `ProductMessage.tsx`, `useProductManagement.ts`, `useShoppingCart.ts` (+teste) e a linha no barrel `hooks/business/index.ts`; `LazyProductManagement` removido de `LazyRoutes.tsx` (sem consumidor). Teste GAP tautológico convertido em regressão real (`fs.existsSync`). `WhatsAppTemplatesManager`, `useRecommendedProducts` e a tabela local `products` ficam intocados. Gates: tsc 0 · typecheck-ratchet 0 novas · lint-ratchet 1189→1125 (novas 0, -64 de dívida) · hook test 94/94 · 2026-09-11

- E04 · Helpers únicos: `catalogShared.tsx` (formatPrice/formatStock/handleImageError/ProductImage, com fallback via `primary_image_fallback_url`); `ExternalProductCard.tsx` e `ProductDetailDialog.tsx` passam a importar de lá; `ContactResult` unificado (só existe em `useSendProduct.ts`, `sendProductUtils.ts` reexporta). 3 testes novos. Achados de gate: warning `react-refresh/only-export-components` (arquivo novo) resolvido seguindo o precedente do repo (`talkxShared.tsx` já usa o mesmo `eslint-disable`); 1 violação `set-state-in-effect` pré-existente da `ProductDetailDialog.tsx` deslocou de linha (removi ~19 linhas de duplicata acima) e foi reancorada no baseline (mesma dívida, 1125→1125, 0 novas) — mesmo padrão já usado no repo (PR #247). Gates: tsc 0 · lint-ratchet 0 novas · catalog+hook tests 97/97 · 2026-09-11

- E05 · `useExternalCatalog` sem dívida de hooks (nenhuma existia no arquivo — `ESTADO_INICIAL.md` já registrava 0; a etapa preparou a API para E06/E17/E26/E54): `ready` (boolean) + `filters` (state separado) viraram um único `filters: CatalogFilters | null` (`null` = ainda não pedido); `fetchCategories`/`fetchSuppliers` passam a só inicializar `filters` se ainda nulo (`f ?? {}`) — preserva exatamente o acoplamento testado ("concurrent fetchProducts and fetchCategories"). Novos campos aditivos: `isInitialLoading`, `isFetching`, `invalidate()`. `loading` inalterado. Gates: tsc 0 · eslint no arquivo 0 · lint-ratchet 1125/1125 (0 novas) · hook test 94/94 · 2026-09-11

- E04 · `catalogShared.tsx` (formatPrice/formatStock/handleImageError/ProductImage com fallbackSrc) reusado por ExternalProductCard e ProductDetailDialog; `ContactResult` unificado em `useSendProduct.ts`. 3 testes novos. Commit `31a818e8`.
- E05 · `useExternalCatalog`: `ready`+`filters` colapsados em `filters: CatalogFilters | null`; `isFetching`/`isInitialLoading`/`invalidate()` aditivos; contrato público e o acoplamento das 3 queries (uma chamada libera as 3) preservados — coberto pelo teste de concorrência. Commit `b3b06b6e`.
- E06 · Zero `set-state-in-effect`/`exhaustive-deps`/`no-restricted-imports` no módulo: `useExternalProduct` (novo, em `useExternalCatalog.ts`) substitui o padrão `useState+useEffect+fetchProduct` em `ProductDetailDialog` e `SendProductDialog`; `SendProductDialog` ajusta `selectedImages` durante o render (padrão oficial do React, sem effect/ref) em vez de `useEffect`; `ExternalProductCatalog`/`ExternalProductManagement` documentam e suprimem pontualmente o `exhaustive-deps` do debounce (reescrita completa com `useReducer` fica para a F3, quando a tela é refeita — reescrever agora seria descartado); `useSendProduct.ts` move a query de contatos para `useCatalogContactSearch.ts` em `src/hooks/integrations/` (camada de apresentação não fala com Supabase direto). Gates: tsc 0 · typecheck-ratchet 0 · lint-ratchet 1118→1114 (novas 0, -4) · 97/97 testes · 2026-09-11

- E07 · Suíte de testes base do módulo (rede de segurança antes do redesign): `sendProductUtils.test.ts` (12 — groupVariantsByColor, buildMessage nos 3 templates + esgotado + variante selecionada + campos ausentes, collectAllImages), `ContactSelectionStep.test.tsx` (6 — botão desabilitado sem contato, habilita com nome, vazio/busca-sem-resultado, lista, "Enviando..."), `ExternalProductManagement.test.tsx` (6 — título+contagem, cards, debounce da busca, toggle grade/lista, vazio, erro). Gates: tsc 0 · typecheck-ratchet 0 · lint-ratchet 1118/1114 (novas 0) · 121/121 testes (5 arquivos) · 2026-09-11

- E07 · Suíte de testes base do módulo antes do redesign: `sendProductUtils.test.ts` (12 — groupVariantsByColor, buildMessage nos 3 templates + esgotado + variante selecionada + campos ausentes, collectAllImages), `ContactSelectionStep.test.tsx` (6 — botão desabilitado sem contato, habilita com nome, estados vazio/sem-resultado, lista de resultados, "Enviando..."), `ExternalProductManagement.test.tsx` (6 — título+contagem, cards, debounce da busca, toggle grade/lista, vazio, erro). Total do módulo: 121/121. Gates: tsc 0 · typecheck-ratchet 0 · lint-ratchet 1118/1114 (0 novas) · 2026-09-11

- E08 · Diagnóstico do envio ponta a ponta — ver `docs/catalogo/ENVIO_E2E.md`. Confirmado por leitura de código: mediaUrl externo (`imagedelivery.net`) passa sem re-upload por `resolvePrivateBucketUrl`; `external_id` é gravado pelo backend, independente do frontend (o "fix #209" do plano original não correspondia a nada real — corrigido). Nunca exercitado em produção (2.218 imagens enviadas, 100% via Storage do próprio ZAPP, 0% externas). Envio real ao vivo não foi possível nesta sessão: `message-delivery` exige sessão de usuário autenticado (sem fixture de login neste repo) e o MCP `EVO API - MCP` retornou 401 (apikey inválida para `wpp2`) — infra fora do escopo do Catálogo, sinalizado. · 2026-09-12

## Fase 1 — Design system (carvão)

- E11 · Classes `.catalog-*` em `components.css`: card, media (fundo branco), 5 badges de status, chip de cor, chip de categoria (+ativo), preço, rail (300/320px), thumb de galeria, moldura de preview WhatsApp. Nenhum token novo — só `hsl(var(--…))` do carvão existente. `COMPONENTES.md` criado. Build OK · 2026-09-12

- E12 · `ProductBadge` + `resolveProductBadge`: 5 estados com prioridade real (esgotado > mais vendido > novidade > destaque > em estoque); renderiza correto mesmo com flags `undefined` (cai em instock/out). 8 testes de prioridade. tsc 0 · 2026-09-12

- E13 · `ColorChips`/`ColorSwatch`: chips do card ("+N" quando excede max) e bolinha de cor do detalhe; aceita string ou objeto (`color_swatches` da E21). Sem `TooltipProvider` global no app — usa `title` nativo em vez de Radix Tooltip. 13 testes (5 novos). tsc 0 · 2026-09-12

- E14 · `PriceTag`/`StockPill`/`LowStockPill`: preço com sugerido riscado condicional; estoque verde/vermelho (trata qty<=0 e flag stockout inconsistentes com "ou"); baixo estoque âmbar só entre 1 e threshold (10 por padrão, o mesmo limiar dos 308 produtos reais). 10 testes novos (23 no arquivo). tsc 0 · 2026-09-12

- E15 · `ProductThumb`: skeleton até `onLoad`, `srcSet` real do Cloudflare Images (variantes confirmadas via CF Images API: thumbnail 150, small 300, card 400, medium/public 600, large 1200 — todas JPEG), fallback em cascata `src`→`fallbackSrc`→ícone. Substitui `ProductImage` (removido, sem consumidor) nos 3 usos reais (2 em `ExternalProductCard.tsx`, 1 em `ProductDetailDialog.tsx`). 5 testes novos.
- E16 · `FavoriteButton`: coração com `aria-pressed`, animação de escala respeitando `useReducedMotion`, `stopPropagation` (não abre o card ao favoritar), estado `busy`. Ainda sem persistência (E27) — não montado em nenhum card. 4 testes novos.
Total do módulo: 150/150. tsc 0 · lint-ratchet 0 novas · 2026-09-12

- E17 · `CatalogKpiStrip` (reuso de `KpiCard`): `KpiCard`/`KpiCardSkeleton` ganham prop `compact` opcional (72px, sem mini-barras, ícone à esquerda) sem quebrar os 9 usos existentes no Talk X (confirmado: 37 testes de TalkX.test.tsx/talkxExport continuam verdes). Strip renderiza só os 6 campos numéricos presentes em `CatalogStats` (nada de "—" decorativo) — hoje sempre oculto, já que `catalog_stats` só existe na E24. 5 testes novos. Total do módulo: 155/155. tsc 0 · lint-ratchet 0 novas · 2026-09-12

- E18 · `CategoryChips` + `CatalogFilterBar`: chips de categoria ordenados por `products_count`, excedente em `DropdownMenu` ("Mais"). `CatalogFilterBar` compõe `FilterBarV2` do Talk X 100% via `rightSlot` (switch "Em estoque" + botão "Filtros avançados" com contador) — **zero mudança em `FilterBarV2`**, mais simples do que as props extras que o plano original previa (`switches[]`/`extraRight`), corrigido no plano. 14 testes novos. Total do módulo: 164/164. tsc 0 · lint-ratchet 0 novas · 2026-09-12

- E19 · `MetaTile`/`SectionCard` (2 primitivos novos). Achado que evitou duplicação: `RailAction` e `RailCard` (com prop `glow`) **já existem** em `talkxShared.tsx`, cobrindo exatamente "ações rápidas do rail" e "banner com glow" — cancelados `RailBanner`/`RailActionRow` do escopo original, plano corrigido. `TalkXPrimaryButton`/`GhostButton` confirmados reutilizáveis direto (sem wrapper) para Enviar/Ver detalhes. 2 testes novos. Total do módulo: 166/166. tsc 0 · lint-ratchet 0 novas · 2026-09-12

- E20 · Regras de motion/densidade/responsivo documentadas em `COMPONENTES.md` (stagger igual ao `DashboardCard.tsx` confirmado, breakpoints, modais, tipografia) — telas reais só existem a partir da F3, então esta etapa é o contrato para quando forem construídas. Fase 1 completa (E11-E20). 2026-09-12

## Fase 2 — Backend

- E22 · Filtros avançados na edge `promogifts-catalog`: `is_featured/is_new/is_bestseller/is_kit/allows_personalization/low_stock/price_min/price_max/color/material/has_engraving`. Categoria com descendentes via `categories.path like '<path>%'` (path confirmado: caminho materializado por uuid). `color`/`material` tratam os **dois formatos reais** do jsonb (achado ao inspecionar o banco: 17.756 elementos string + 802 objetos `{nome, swatch_url}`, ~4% do fornecedor XBZ) — `contains` em vez de `ilike` (Postgres rejeita `ilike` sem cast em jsonb), confirmado por SQL direto (411 e 100 matches nos dois formatos). **Limitação registrada:** `supabase/functions/**` não está no `tsconfig.app.json` nem tem `deno` disponível neste ambiente — a única validação real de sintaxe/tipo Deno é o próprio deploy; código revisado à mão, sem chamada ao vivo. `CatalogFilters` (hook) tipado com os novos campos. Manifest regenerado (66 funções). tsc 0 · lint-ratchet 0 novas · hook 94/94 · 2026-09-12

- E23 · Busca full-text real na edge: `search_vector` confirmado 100% preenchido (8.031/8.031) e mantido por gatilho — sem fallback ilike (seria código morto). Achado real: o gatilho aplica `unaccent()` **antes** do `to_tsvector`, então uma busca acentuada gerava léxico diferente do vetor (confirmado por SQL: `açucareiro` → `'açucareir'`, vetor tem `'acucareir'`) — `stripDiacritics()` (equivalente JS do `unaccent`) aplicado ao termo antes de `.textSearch()`. `order_by` += `order_count`. **`relevance` cancelado desta etapa** e documentado como pendente (exigiria RPC de rank dedicada — decisão de arquitetura maior, não fingida aqui). Latência real via `EXPLAIN ANALYZE`: 7ms com o índice GIN `idx_products_search_vector`. tsc 0 · lint-ratchet 0 novas (sem mudança no hook) · manifest regenerado · 2026-09-12

- E24 · RPC `public.zapp_catalog_stats()` (security definer, read-only, aplicada via `apply_migration` e testada com chamada real) + ação `catalog_stats` na edge (cache 60s por isolate). Retorna total/in_stock/featured/new_30d/bestseller/kits/low_stock/categories_root/suppliers_active/last_sync_at/last_update_at/by_month (7 meses). Achado real: `v_catalog_stats` existente só tinha 2 campos (total_variants, total_suppliers) — insuficiente, RPC nova necessária. Achado real #2: 27 categorias raiz **ativas**, não 28 (1 inativa/soft-deleted) — corrigido no plano (3 ocorrências). `CatalogStats` (tipo completo, sem campos opcionais) movido para `useExternalCatalog.ts` (camada de dados) — `catalogShared.tsx` reimporta em vez de duplicar; `useCatalogStats()` hook novo, `staleTime` 5min. 4 testes do `CatalogKpiStrip` ajustados (mock completo via `mockStats()`, já que a RPC sempre retorna o objeto inteiro). Manifest regenerado. tsc 0 · lint-ratchet 0 novas · vitest 166/166 · 2026-09-12

- E25 · `list_categories`/`list_suppliers` enriquecidos (`icon`, `color_hex`, `image_url`, `products_count`, `level`, `path`, `full_path_readable`, `display_order` / `trading_name`, `logo_url`, `is_product_supplier`, `low_stock_threshold`), filtrando `is_active`+`deleted_at is null` (categorias) e `active` (fornecedores) — confirmado por SQL: ícones já são nomes reais do `lucide-react` (ex. `Umbrella`, `Wrench`, `Sprout`). **2 achados reais:** (1) `active=true` retorna **5 fornecedores**, não os 4 "com produto ativo" da RPC da E24 — são definições diferentes de "ativo" (registro vs. tem produto), documentado para não confundir na F3; (2) **nenhum dos 5 fornecedores tem `logo_url`** hoje (todos `null`) — a UI vai precisar de fallback sem logo desde o início, não é um caso raro. `ARQUITETURA.md` **criado retroativamente** nesta etapa (deveria ter sido feito na E10 e atualizado na E24 — nenhum dos dois aconteceu; corrigido no plano). Manifest regenerado. tsc 0 · lint-ratchet 0 novas · vitest 166/166 · 2026-09-12

- E26 · Ação `bootstrap` na edge (categorias + fornecedores + stats via `Promise.all`, reaproveitando o cache de 60s do `catalog_stats`) — reduz de 3 chamadas pra 1 por abertura de tela (junto com `list_products`, fica em 2 no total). Frontend: `useCatalogBootstrapQuery` interna (mesma `queryKey` em todo lugar) compartilhada por `useExternalCatalog()` e `useCatalogStats()` — o react-query dedupe garante 1 request mesmo com os dois hooks montados juntos. `placeholderData: keepPreviousData` na lista de produtos (sem flash na paginação). 5 testes do hook precisaram de ajuste real (mockavam `list_categories`/`list_suppliers` isolados — contrato de rede mudou de propósito); contrato público (`.categories`/`.suppliers`) inalterado. `staleTime` da lista mantido em 5min (não reduzido pra 2min como o plano original sugeria — sem motivo pra mudar o que já funciona). Prefetch no hover de "Próxima" adiado pra E45 (não existe botão ainda). tsc 0 · lint-ratchet 0 novas (e -2 de dívida corrigida no caminho) · vitest 166/166 · 2026-09-12

- E27 · Tabela `catalog_favorites` no ZAPP (primeira escrita no banco do ZAPP, não só no PromoGifts). Aplicada via `db_apply_migration` (versão `20260913013153`), seguindo o padrão real de `favorite_contacts` (checado antes de escrever): `id` próprio + `UNIQUE(user_id, product_id)` — não a PK composta que o plano original previa —, 1 policy `FOR ALL`. `schema-catalog.json` regenerado de verdade (139 tabelas). `schema-manifest.json` deixado pro workflow `types-sync.yml` (achado: já existe e dispara sozinho em push no `main` tocando `supabase/migrations/**`, confirmado lendo o próprio arquivo). `src/integrations/supabase/types.ts` **precisou de edição manual** (diferente do manifest — bloqueia `tsc` agora, não só depois do merge): entrada `catalog_favorites` inserida à mão no formato do codegen, entre `campaigns` e `channel_connections`. `useCatalogFavorites()` (list + toggle otimista com rollback), seguindo a convenção real de `favorite_contacts` (`supabase.auth.getUser()` direto). **Achado real de bug no próprio hook:** `queryClient.invalidateQueries()` estava sem `await` — o refetch em segundo plano competia com quem chamasse `toggle()` em sequência; corrigido para `await`. 5 testes novos, com um teste genuinamente intermitente até eu isolar corretamente o desmonte entre casos e trocar uma asserção síncrona por `waitFor` (confirmado 20/20 execuções depois). tsc 0 · lint-ratchet 0 novas (-6 dívida) · vitest 171/171 · 2026-09-13

- E28 · Tabela `catalog_send_events` (log de envios). Mesmo achado da E27, confirmado de novo: `agent_id` referencia `profiles(id)` (checado contra `csat_surveys`, a tabela real mais parecida), não `auth.users(id)`; RLS com 2 policies (insert `WITH CHECK`/select restrito a "meus próprios ou admin/supervisor"), não a policy única que o plano original previa. `logCatalogSendEvent()` novo em `useCatalogContactSearch.ts` (mesmo arquivo que já isola Supabase de `src/components/catalog/**`). `useSendToContact` grava 1 evento real ao final do envio: `status` calculado das falhas reais (`sent`/`partial`/`failed`), `message_ids` coletados de `sendOutboundMessage`, `agentId` resolvido via `useAuth().profile?.id` (não `auth.users.id`) em `SendProductDialog.tsx`. Isso quebrou `ExternalProductManagement.test.tsx` de verdade (sem `AuthProvider`) — corrigido com o mock padrão do projeto; primeira tentativa errou (implementação inicial tipada trava a assinatura contra o spread `...args`), corrigida pra `vi.fn()` puro + `mockReturnValue`. `types.ts` e `schema-catalog.json` atualizados (140 tabelas). **Adiado conscientemente:** `useCatalogRecentSends(limit)` (sem tela "Enviados recentemente" ainda) e teste dedicado da lógica de `status` (pendência real, registrada, não bloqueante). tsc 0 · lint-ratchet 0 novas · vitest 171/171 · 2026-09-13

## Fase 3 — Topo da tela A

- E31 · Layout `[1fr_300px]` e scroll único. Achado real ao ler `ViewContainer.tsx`: o próprio componente documenta "views must be w-full min-w-0 flat containers — no overflow-y-auto at root" e é o único dono do scroll (`overflow-y-auto` na `ViewContainer`, catalog está em `COMPACT_GUTTER_VIEWS`). O `<ScrollArea className="h-[calc(100vh-320px)]">` interno da grade de produtos era um **segundo scroller real**, não hipotético — removido. `max-w-7xl mx-auto` removido (conflitava com o grid `xl:grid-cols-[1fr_300px]` de largura total). Rail adicionado só como estrutura (`<aside className="catalog-rail sticky top-4">`), sem conteúdo — isso é F5. **Sem screenshots 1920/1440** (sub-etapa 7 do plano): app não está deployado/rodando em lugar acessível pra capturar — adiado, registrado, não escondido. tsc 0 · lint-ratchet 0 novas · vitest 171/171 · build OK · 2026-09-13

- E32 · Header do modulo. `ModuleHeader`+`IconTile` reusados de `talkxShared.tsx` (mesmo componente ja usado pelo Talk X, nenhum novo criado). Subtitulo com `total` real de `useCatalogStats()` (E24), fallback pro `totalProducts` local enquanto `stats` carrega. Chip de sincronizacao (`SyncStatusChip`) escrito do zero seguindo o padrao ponto+texto ja usado em `TalkXSegments`/`TalkXCampaignRunning` — nao existe um componente `StatusChip` generico no projeto pra reusar. **2 regras reais do eslint descobertas na hora:** `react-hooks/purity` proibe `Date.now()` no corpo do render; `react-hooks/set-state-in-effect` proibe `setState` sincrono dentro de effect. Resolvido com o inicializador preguicoso do `useState` (unica excecao sancionada pra leitura de valor impuro) + `key={lastSyncAt}` no ponto de uso pra forcar recalculo quando o valor muda — sem effect nenhum. Botao "+ Novo Produto" **omitido** (nao criado): busquei confirmacao de uma rota publica real de cadastro em promogifts.com.br e nao achei nenhuma — o dominio e vitrine de cliente, nao painel admin; seguindo a instrucao explicita do plano ("se nao existir rota publica, ocultar o botao"). `<Badge>` do total antigo removido (virou texto no subtitulo do `ModuleHeader`) — import morto limpo junto. Achado real de bug no proprio codigo escrito nesta etapa: a string do chip "velho" saiu sem acentos (`Ultima sincronizacao`) na primeira escrita — pego pelo proprio teste que escrevi, corrigido pra "Última sincronização". 2 testes novos (chip fresco/velho) + 1 assercao existente atualizada pro texto novo do subtitulo. tsc 0 · lint-ratchet 0 novas · vitest 173/173 · 2026-09-13

- E33 · KPIs reais. CatalogKpiStrip (ja existia da F1/E17) ligado direto a useCatalogStats() (E24) — nenhum componente novo criado, so a fiacao. Achado real: o plano previa clique em qualquer KPI aplicando filtro, mas so 3 dos 6 tem filtro booleano equivalente (Em estoque -> onlyInStock, ja existente; Em destaque -> is_featured, novo; Novidades -> is_new, novo). Categorias/Fornecedores/Total sao so numeros informativos — handleKpiSelect trata como no-op consciente pra esses 3, nao "botao morto" por omissao. Estado de erro do stats -> AlertCard (ja existia em talkxShared.tsx, reusado) em vez do strip, sem "—" decorativo. Achado real de teste: o effect de debounce roda no mount tambem (array de deps nao vazio ainda dispara na 1a renderizacao) — um teste que fazia mockClear() logo apos renderManagement() pegava falso-positivo porque essa chamada de mount ainda nao tinha assentado; corrigido esperando 350ms antes do clear. is_featured/is_new adicionados ao buildFilters/deps/hasFilters/clearFilters (campos da E22 finalmente wireados no frontend). 2 testes novos (clique aplica filtro; clique em KPI nao-acionavel nao aplica nada). tsc 0 · lint-ratchet 0 novas · vitest 175/175 · build OK · 2026-09-13

- E34c · Wiring final: CategoryChips + deep link no ExternalProductManagement.tsx. categoryId inicializado via inicializador preguicoso do useState lendo a URL 1x no mount (nao effect - evita corrida com o 1o render); malformado/duplicado normalizado num effect de mount separado (mesmo padrao de needsNormalization do talkxWizardRoute). handleCategoryChange unico ponto de mudanca - chip e select (ja existente) chamam a mesma funcao, mantendo os dois sincronizados e refletindo na URL via replaceState. Achado real de teste: getAllByText("Canetas") pegava 3 elementos - o chip real E 2 badges (div, nao clicaveis) nos cards de produto mockados, mostrando o nome da categoria; o teste original clicava no badge errado (ultimo da lista), testando nada. Corrigido para getByRole("button", { name: ... }), que exclui os badges nao-interativos. Segundo achado: o id de categoria mockado nos testes existentes ("cat1") nao e um UUID de verdade - parseCatalogCategoryRoute rejeita isso como malformado por design (correto para dados reais); o teste de deep link precisou de um id UUID real, nao do id simplificado compartilhado com os outros testes do arquivo. 2 testes novos (clique aplica filtro + URL; URL no mount ja aplica o filtro). Suite completa do modulo rodada (nao so os arquivos tocados): tsc 0, lint-ratchet 0 novas, vitest 180/180 (6 arquivos), vite build OK. E34 completa (E34a+b+c).

## E34–E35–E37–E39 — reconciliacao de conflito real (2026-09-16)
Outra sessao (identidade git "Promo Bot", branch `feat/catalog-f3-screens` original antes de eu renomear por colisao) implementou E34+E35+E37+E39 de forma independente e concorrente, sem saber do meu trabalho em andamento no clone isolado. Ao abrir o PR #415 contra o `main` ja avancado, o merge teve **conflito real** (nao so metadado, como nas etapas anteriores) em `ExternalProductManagement.tsx`: o `main` tinha um `CategoryChips` LOCAL duplicado (nao reusava `catalogShared.tsx`, sem icone, sem deep link, estilizado com classes Tailwind ad-hoc em vez do `.catalog-category-chip` do design system da F1). Resolvido a favor do componente compartilhado (E18/F1, ja com 51 testes proprios) + E34a (deep link) + E34b (icone) - estritamente mais completos. A funcao local duplicada foi removida.
- E35: `viewMode` persiste em `localStorage catalog.view`; `orderBy`/`ascending` em `sessionStorage catalog.order_by/catalog.ascending`; `clearFilters` reseta sort — **implementado pela outra sessao, nao tocado por mim, preservado como estava no merge**.
- E37: dropdown "Ordenar por" com 7 opções; linha de resultados com `tabular-nums` — **idem, preservado**.
- E39: estados vazios distintos (com filtros vs catálogo vazio); error state via `AlertCard tone="danger"` — **idem, preservado**.
Gates completos rodados de novo pos-reconciliacao antes do push.

## FASE 5 — Rail direito (2026-09-23)
- **E51 · Banner do rail** — `CatalogRail.tsx` criado (o `<aside className="catalog-rail">` nasceu vazio na E31 e continuava vazio). Copy isolada em `CATALOG_RAIL_COPY`. Foto do banner vem do 1º produto `is_featured` **com imagem** da lista já carregada; destaque sem foto é ignorado e sem destaque nenhum o bloco da foto some — nada de imagem fake. A marca "SUA MARCA AQUI" do mock não entrou (é placeholder de mockup). CTA aplica o filtro de novidades.
  - **Desvio consciente do plano:** o item 1 pedia o destaque "cacheado no bootstrap", mas o `bootstrap` da edge devolve só `categories/suppliers/stats` — fazer isso exigiria alterar e redeployar a edge em produção por causa de uma foto. Usa-se a lista já carregada na tela; mesmo resultado visual, zero risco de deploy.
- **E52 · Gráfico mensal** — `recharts BarChart` com `by_month` do `catalog_stats`, últimos 7 meses, meses abreviados em pt-BR (`date-fns/ptBR`), barra do mês atual em `--primary` e as demais em `primary/40`, eixo Y com 4 ticks, `RailCard glow` só neste card. Delta "+X%" só quando o mês anterior tem base > 0 — sem isso qualquer produto novo viraria `+Infinity%`. "Ver relatórios →" **omitido**: existe view `reports` no dashboard, mas nenhum relatório de catálogo — o próprio plano manda ocultar nesse caso.
- **E53 · Contagens do rail** — 4 linhas (`ul/li`) com `MetaRow` + valores reais de `catalog_stats`, `tabular-nums`, skeleton de 4 linhas. As 3 primeiras são clicáveis e aplicam filtro; **"Fornecedores ativos" não é clicável** porque é contagem agregada, não filtro booleano da listagem (mesmo achado da E33 sobre os KPIs não-acionáveis). As chaves do rail são `keyof CatalogStats` de propósito, para o caller reusar `handleKpiSelect` sem adaptador.
- **E56 · Enviados recentemente / Mais enviados** — `useCatalogRecentSends.ts` criado; este hook estava **adiado desde a E28** por não existir tela que o consumisse, e o rail é essa tela. Lê `catalog_send_events` (janela de 30 dias), monta os 5 recentes e o top 3 por `product_id` agregado no cliente (não há RPC de agregação para a tabela). Status `sent/partial/failed` → tom do ponto do `RecentList`. Clicar reabre o envio: como a tabela guarda só o id, o pai busca o produto completo via `fetchProduct(id)` antes de abrir o diálogo.
  - **RLS conferida de verdade** (não presumida): a policy de SELECT é `agent_id IN (profiles do auth.uid()) OR is_admin_or_supervisor(auth.uid())` — por isso o hook **não** filtra por `agent_id` de novo, senão um supervisor deixaria de ver os envios da equipe.
  - **Sem dados hoje**: `catalog_send_events` tem 0 linhas nos últimos 30 dias, então a seção nasce oculta em produção — exatamente o que o item 3 do plano previu. Os testes cobrem com mock.

### Não implementado nesta leva (e por quê)
- **E54 · "Sincronizar catálogo"** — **inexequível como especificado**. A edge `promogifts-catalog` expõe apenas `list_products / get_product / list_categories / list_suppliers / catalog_stats / bootstrap`; não há ação de sincronização e o catálogo é somente-leitura no ZAPP. Implementar o botão criaria um controle que não faz nada. Precisa de decisão de escopo antes.
