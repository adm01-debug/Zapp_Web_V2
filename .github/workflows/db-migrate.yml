# ==================================================================
# ZAPP-WEB · Migration controlada do banco oficial
# ==================================================================
# Executa somente por workflow_dispatch na main. O dry-run e obrigatorio
# antes de uma aplicacao: ele comprova o destino, exige que exista exatamente
# uma migration local ausente e publica apenas hashes estruturais seguros.
# Somente alvos com contrato runtime explicito neste arquivo sao aceitos. Para
# aplicar, o operador confirma o hash integral do dry-run; o workflow consulta
# o runtime novamente para fechar a janela TOCTOU.
# ==================================================================

name: DB Migrate (production)

on:
  workflow_dispatch:
    inputs:
      migration_version:
        description: 'Versao exata, com 14 digitos (ex: 20260830170000).'
        required: true
        type: string
      confirm_project_ref:
        description: 'Digite o project-ref oficial para confirmar o destino.'
        required: true
        type: string
      apply:
        description: 'Desmarcado = dry-run; marcado = aplicar a unica migration ausente.'
        required: true
        default: false
        type: boolean
      confirm_runtime_sha256:
        description: 'Para apply: SHA-256 completo exibido pelo dry-run anterior.'
        required: false
        default: ''
        type: string

permissions:
  contents: read

concurrency:
  group: db-migrate-production
  cancel-in-progress: false

env:
  SUPABASE_CLI_VERSION: '2.116.0'
  OFFICIAL_PROJECT_REF: 'tnnnlkbymytvtqngbbqh'

jobs:
  migrate:
    name: Migration oficial com preflight
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Checkout confiavel da main
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.sha }}
          persist-credentials: false

      - name: Provar checkout imutavel
        run: test "$(git rev-parse HEAD)" = "$GITHUB_SHA"

      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: '24'

      - name: Setup Supabase CLI ${{ env.SUPABASE_CLI_VERSION }}
        uses: supabase/setup-cli@46f7f98c7f948ad727d22c1e67fab04c223a0520 # v3.0.0
        with:
          version: ${{ env.SUPABASE_CLI_VERSION }}

      - name: Instalar cliente PostgreSQL
        run: bash scripts/ci/install-postgresql-client.sh

      - name: Provar identidade e validar confirmacoes
        env:
          DESTINO_URL: ${{ secrets.DESTINO_URL }}
          TARGET_VERSION: ${{ inputs.migration_version }}
          CONFIRM_PROJECT_REF: ${{ inputs.confirm_project_ref }}
          CONFIRM_RUNTIME_SHA256: ${{ inputs.confirm_runtime_sha256 }}
          APPLY: ${{ inputs.apply }}
        run: |
          if [ -z "$DESTINO_URL" ]; then
            echo "::error::DESTINO_URL ausente."
            exit 1
          fi
          if [ "$CONFIRM_PROJECT_REF" != "$OFFICIAL_PROJECT_REF" ]; then
            echo "::error::Project-ref de confirmacao incorreto."
            exit 1
          fi
          if ! printf '%s' "$TARGET_VERSION" | grep -Eq '^[0-9]{14}$'; then
            echo "::error::migration_version precisa ter exatamente 14 digitos."
            exit 1
          fi
          if [ "$APPLY" = "true" ] && ! printf '%s' "$CONFIRM_RUNTIME_SHA256" | grep -Eq '^[a-f0-9]{64}$'; then
            echo "::error::Apply exige confirm_runtime_sha256 completo e lowercase."
            exit 1
          fi
          node --input-type=module <<'NODE'
          import {
            carregarIdentidadeEsperada,
            validarDestino,
          } from './scripts/db-audit/database-identity.mjs';
          const identidade = carregarIdentidadeEsperada('scripts/db-audit/database-identity.json');
          const erros = validarDestino(process.env.DESTINO_URL, identidade);
          for (const erro of erros) console.error('ERRO: ' + erro);
          if (erros.length) process.exit(1);
          console.log('OK: destino oficial comprovado por fingerprint.');
          NODE

      - name: Exigir exatamente uma migration ausente
        id: preflight
        env:
          DESTINO_URL: ${{ secrets.DESTINO_URL }}
          TARGET_VERSION: ${{ inputs.migration_version }}
          APPLY: ${{ inputs.apply }}
        run: |
          umask 077
          find supabase/migrations -maxdepth 1 -type f -name '*.sql' \
            -printf '%f\n' | cut -c1-14 | sort -u > /tmp/local-versions
          psql "$DESTINO_URL" -X -v ON_ERROR_STOP=1 -At \
            -c "SELECT version FROM supabase_migrations.schema_migrations ORDER BY version" \
            > /tmp/remote-versions
          comm -13 /tmp/local-versions /tmp/remote-versions > /tmp/remote-only-versions
          REMOTE_ONLY_COUNT=$(wc -l < /tmp/remote-only-versions | tr -d ' ')
          if [ "$REMOTE_ONLY_COUNT" != "0" ]; then
            echo "::error::O ledger remoto possui versions sem arquivo local; db push foi bloqueado antes do CLI."
            echo "remote_only_count=$REMOTE_ONLY_COUNT"
            exit 1
          fi
          comm -23 /tmp/local-versions /tmp/remote-versions > /tmp/missing-versions
          MISSING_COUNT=$(wc -l < /tmp/missing-versions | tr -d ' ')
          MISSING_VERSION=$(sed -n '1p' /tmp/missing-versions)
          if [ "$MISSING_COUNT" = "1" ] && [ "$MISSING_VERSION" = "$TARGET_VERSION" ]; then
            NEEDS_APPLY=true
          elif [ "$MISSING_COUNT" = "0" ] && grep -Fxq "$TARGET_VERSION" /tmp/remote-versions; then
            NEEDS_APPLY=false
          else
            echo "::error::O conjunto de versions ausentes nao corresponde exatamente ao alvo informado."
            echo "missing_count=$MISSING_COUNT"
            exit 1
          fi
          FILE_COUNT=$(find supabase/migrations -maxdepth 1 -type f \
            -name "${TARGET_VERSION}_*.sql" | wc -l | tr -d ' ')
          if [ "$FILE_COUNT" != "1" ]; then
            echo "::error::A versao alvo precisa mapear para exatamente um arquivo local."
            exit 1
          fi
          TARGET_FILE=$(find supabase/migrations -maxdepth 1 -type f \
            -name "${TARGET_VERSION}_*.sql" -printf '%f\n')
          TARGET_NAME=${TARGET_FILE#${TARGET_VERSION}_}
          TARGET_NAME=${TARGET_NAME%.sql}
          if [ "$APPLY" = "true" ] && [ "$NEEDS_APPLY" != "true" ]; then
            echo "::error::A migration alvo ja esta registrada; apply repetido foi bloqueado."
            exit 1
          fi
          echo "needs_apply=$NEEDS_APPLY" >> "$GITHUB_OUTPUT"
          echo "target_name=$TARGET_NAME" >> "$GITHUB_OUTPUT"
          echo "OK: estado da migration alvo confirmado: version=$TARGET_VERSION needs_apply=$NEEDS_APPLY"

      - name: Provar paridade integral quando a versao ja existe
        if: steps.preflight.outputs.needs_apply == 'false'
        env:
          DESTINO_URL: ${{ secrets.DESTINO_URL }}
        run: node scripts/db-audit/check-migration-drift.mjs

      - name: Provar estado runtime antes do push
        id: runtime
        env:
          DESTINO_URL: ${{ secrets.DESTINO_URL }}
          CONFIRM_RUNTIME_SHA256: ${{ inputs.confirm_runtime_sha256 }}
          APPLY: ${{ inputs.apply }}
          TARGET_VERSION: ${{ inputs.migration_version }}
          NEEDS_APPLY: ${{ steps.preflight.outputs.needs_apply }}
        run: |
          case "$TARGET_VERSION" in
            20260830170000)
              RUNTIME=$(psql "$DESTINO_URL" -X -v ON_ERROR_STOP=1 -At -c "
                WITH fn AS (
                  SELECT p.oid, p.prosecdef, p.provolatile, p.proconfig,
                         pg_get_functiondef(p.oid) AS body
                  FROM pg_proc p
                  JOIN pg_namespace n ON n.oid = p.pronamespace
                  WHERE n.nspname='public' AND p.proname='get_team_profiles'
                    AND pg_get_function_identity_arguments(p.oid)=''
                )
                SELECT json_build_object(
                  'server_major', current_setting('server_version_num')::int / 10000,
                  'database', current_database(),
                  'function_count', count(*),
                  'security_definer', bool_and(prosecdef),
                  'stable', bool_and(provolatile='s'),
                  'search_path_fixed', bool_and('search_path=public' = ANY(proconfig)),
                  'filters_active', bool_and(position('where p.is_active = true' in lower(body)) > 0),
                  'runtime_sha256', min(encode(sha256(convert_to(body, 'UTF8')), 'hex'))
                )::text
                FROM fn;")
              ;;
            20260831120000)
              RUNTIME=$(psql "$DESTINO_URL" -X -v ON_ERROR_STOP=1 -At -c "
                WITH relation AS (
                  SELECT c.oid, c.relrowsecurity
                  FROM pg_class c
                  JOIN pg_namespace n ON n.oid = c.relnamespace
                  WHERE n.nspname = 'public'
                    AND c.relname = 'webhook_failures'
                    AND c.relkind IN ('r', 'p')
                ), policy_state AS (
                  SELECT
                    count(*) AS policy_count,
                    count(*) FILTER (WHERE 0 = ANY(pol.polroles)) AS public_policy_count,
                    count(*) FILTER (
                      WHERE pol.polname = 'service_role_full'
                        AND pol.polroles = ARRAY['service_role'::regrole::oid]
                        AND pol.polcmd = '*'
                        AND pg_get_expr(pol.polqual, pol.polrelid) = 'true'
                        AND pg_get_expr(pol.polwithcheck, pol.polrelid) = 'true'
                    ) AS service_role_policy_count
                  FROM pg_policy pol
                  JOIN relation r ON r.oid = pol.polrelid
                ), payload AS (
                  SELECT jsonb_build_object(
                    'server_major', current_setting('server_version_num')::int / 10000,
                    'database', current_database(),
                    'table_count', (SELECT count(*) FROM relation),
                    'rls_enabled', COALESCE((SELECT bool_and(relrowsecurity) FROM relation), false),
                    'policy_count', (SELECT policy_count FROM policy_state),
                    'public_policy_count', (SELECT public_policy_count FROM policy_state),
                    'service_role_policy_count', (SELECT service_role_policy_count FROM policy_state),
                    'anon_acl', COALESCE((SELECT has_table_privilege('anon', oid, 'SELECT, INSERT, UPDATE, DELETE') FROM relation), false),
                    'authenticated_acl', COALESCE((SELECT has_table_privilege('authenticated', oid, 'SELECT, INSERT, UPDATE, DELETE') FROM relation), false),
                    'service_role_crud', COALESCE((SELECT
                      has_table_privilege('service_role', oid, 'SELECT')
                      AND has_table_privilege('service_role', oid, 'INSERT')
                      AND has_table_privilege('service_role', oid, 'UPDATE')
                      AND has_table_privilege('service_role', oid, 'DELETE')
                    FROM relation), false),
                    'service_role_extra', COALESCE((SELECT has_table_privilege(
                      'service_role', oid, 'TRUNCATE, REFERENCES, TRIGGER, MAINTAIN'
                    ) FROM relation), false)
                  ) AS value
                )
                SELECT (value || jsonb_build_object(
                  'runtime_sha256', encode(sha256(convert_to(value::text, 'UTF8')), 'hex')
                ))::text
                FROM payload;")
              ;;
            20260831150000)
              RUNTIME=$(psql "$DESTINO_URL" -X -v ON_ERROR_STOP=1 -At -c "
                WITH source_counts AS (
                  SELECT count(*) FILTER (WHERE media_url ~ '/storage/v1/object/sign/(audio-messages|team-chat-files|whatsapp-media)/') AS legacy_count
                  FROM public.messages
                  UNION ALL
                  SELECT count(*) FILTER (WHERE media_url ~ '/storage/v1/object/sign/(audio-messages|team-chat-files|whatsapp-media)/')
                  FROM public.scheduled_messages
                  UNION ALL
                  SELECT count(*) FILTER (WHERE media_url ~ '/storage/v1/object/sign/(audio-messages|team-chat-files|whatsapp-media)/')
                  FROM public.team_messages
                  UNION ALL
                  SELECT count(*) FILTER (WHERE file_url ~ '/storage/v1/object/sign/(audio-messages|team-chat-files|whatsapp-media)/')
                  FROM public.knowledge_base_files
                ), payload AS (
                  SELECT jsonb_build_object(
                    'server_major', current_setting('server_version_num')::int / 10000,
                    'database', current_database(),
                    'source_count', count(*),
                    'legacy_signed_url_count', sum(legacy_count)
                  ) AS value
                  FROM source_counts
                )
                SELECT (value || jsonb_build_object(
                  'runtime_sha256', encode(sha256(convert_to(value::text, 'UTF8')), 'hex')
                ))::text
                FROM payload;")
              ;;
            20260908180000)
              RUNTIME=$(psql "$DESTINO_URL" -X -v ON_ERROR_STOP=1 -At -c "
                WITH relations AS (
                  SELECT c.relname, c.relrowsecurity
                  FROM pg_class c
                  JOIN pg_namespace n ON n.oid = c.relnamespace
                  WHERE n.nspname = 'public'
                    AND c.relname IN ('crm_contact_links', 'crm_sync_outbox')
                    AND c.relkind IN ('r', 'p')
                ), functions AS (
                  SELECT p.proname, p.prosecdef,
                         COALESCE('search_path=public, pg_temp' = ANY(p.proconfig), false) AS safe_path
                  FROM pg_proc p
                  JOIN pg_namespace n ON n.oid = p.pronamespace
                  WHERE n.nspname = 'public'
                    AND p.proname IN (
                      'enqueue_crm_sync_from_closure', 'claim_crm_sync_outbox',
                      'claim_crm_sync_outbox_by_id', 'complete_crm_sync_outbox',
                      'fail_crm_sync_outbox', 'get_crm_sync_health'
                    )
                ), triggers AS (
                  SELECT count(*) AS n
                  FROM pg_trigger t
                  JOIN pg_class c ON c.oid = t.tgrelid
                  JOIN pg_namespace ns ON ns.oid = c.relnamespace
                  WHERE ns.nspname = 'public'
                    AND c.relname = 'conversation_closures'
                    AND t.tgname = 'trg_enqueue_crm_sync_from_closure'
                    AND NOT t.tgisinternal
                ), payload AS (
                  SELECT jsonb_build_object(
                    'server_major', current_setting('server_version_num')::int / 10000,
                    'database', current_database(),
                    'table_count', (SELECT count(*) FROM relations),
                    'rls_count', (SELECT count(*) FROM relations WHERE relrowsecurity),
                    'function_count', (SELECT count(*) FROM functions),
                    'safe_function_count', (SELECT count(*) FROM functions WHERE prosecdef AND safe_path),
                    'trigger_count', (SELECT n FROM triggers)
                  ) AS value
                )
                SELECT (value || jsonb_build_object(
                  'runtime_sha256', encode(sha256(convert_to(value::text, 'UTF8')), 'hex')
                ))::text
                FROM payload;")
              ;;
            20260908220000)
              RUNTIME=$(psql "$DESTINO_URL" -X -v ON_ERROR_STOP=1 -At -c "
                WITH payload AS (
                  SELECT jsonb_build_object(
                    'server_major', current_setting('server_version_num')::int / 10000,
                    'database', current_database(),
                    'lease_column_count', (
                      SELECT count(*) FROM information_schema.columns
                      WHERE table_schema='public' AND table_name='crm_sync_outbox' AND column_name='lease_token'
                    ),
                    'new_complete_count', (
                      SELECT count(*) FROM pg_proc p JOIN pg_namespace n ON n.oid=p.pronamespace
                      WHERE n.nspname='public' AND p.proname='complete_crm_sync_outbox'
                        AND pg_get_function_identity_arguments(p.oid)='p_id uuid, p_lease_token uuid, p_interaction_id text, p_contact_id text, p_company_id text'
                    ),
                    'old_complete_count', (
                      SELECT count(*) FROM pg_proc p JOIN pg_namespace n ON n.oid=p.pronamespace
                      WHERE n.nspname='public' AND p.proname='complete_crm_sync_outbox'
                        AND pg_get_function_identity_arguments(p.oid)='p_id uuid, p_interaction_id text, p_contact_id text, p_company_id text'
                    ),
                    'new_fail_count', (
                      SELECT count(*) FROM pg_proc p JOIN pg_namespace n ON n.oid=p.pronamespace
                      WHERE n.nspname='public' AND p.proname='fail_crm_sync_outbox'
                        AND pg_get_function_identity_arguments(p.oid)='p_id uuid, p_lease_token uuid, p_error_code text'
                    ),
                    'state_constraint_count', (
                      SELECT count(*) FROM pg_constraint
                      WHERE conrelid='public.crm_sync_outbox'::regclass
                        AND conname IN ('crm_sync_outbox_lease_state','crm_sync_outbox_success_state','crm_sync_outbox_phone_state')
                    )
                  ) AS value
                )
                SELECT (value || jsonb_build_object(
                  'runtime_sha256', encode(sha256(convert_to(value::text, 'UTF8')), 'hex')
                ))::text FROM payload;")
              ;;
            20260909120000)
              RUNTIME=$(psql "$DESTINO_URL" -X -v ON_ERROR_STOP=1 -At -c "
                WITH expected_functions AS (
                  SELECT p.oid, p.proname, p.prosecdef,
                         COALESCE('search_path=public, pg_temp' = ANY(p.proconfig), false) AS safe_path
                  FROM pg_proc p
                  JOIN pg_namespace n ON n.oid = p.pronamespace
                  WHERE n.nspname='public'
                    AND p.proname IN (
                      'merge_contacts_atomic', 'upsert_crm_contact_link_guarded',
                      'cleanup_crm_sync_outbox'
                    )
                ), payload AS (
                  SELECT jsonb_build_object(
                    'server_major', current_setting('server_version_num')::int / 10000,
                    'database', current_database(),
                    'table_count', (
                      SELECT count(*) FROM pg_class c JOIN pg_namespace n ON n.oid=c.relnamespace
                      WHERE n.nspname='public' AND c.relname IN ('crm_contact_links','crm_sync_outbox')
                        AND c.relkind IN ('r','p')
                    ),
                    'constraint_count', (
                      SELECT count(*) FROM pg_constraint
                      WHERE conrelid='public.crm_sync_outbox'::regclass
                        AND conname IN (
                          'crm_sync_outbox_payload_size','crm_sync_outbox_external_ids_bounded',
                          'crm_sync_outbox_lease_state','crm_sync_outbox_success_state',
                          'crm_sync_outbox_phone_state'
                        )
                    ),
                    'validated_constraint_count', (
                      SELECT count(*) FROM pg_constraint
                      WHERE conrelid='public.crm_sync_outbox'::regclass AND convalidated
                        AND conname IN (
                          'crm_sync_outbox_payload_size','crm_sync_outbox_external_ids_bounded',
                          'crm_sync_outbox_lease_state','crm_sync_outbox_success_state',
                          'crm_sync_outbox_phone_state'
                        )
                    ),
                    'function_count', (SELECT count(*) FROM expected_functions),
                    'safe_function_count', (
                      SELECT count(*) FROM expected_functions WHERE prosecdef AND safe_path
                    ),
                    'flag_count', (SELECT count(*) FROM public.feature_flags WHERE key='crm.integration'),
                    'flag_enabled', COALESCE((
                      SELECT enabled FROM public.feature_flags WHERE key='crm.integration'
                    ), false)
                  ) AS value
                )
                SELECT (value || jsonb_build_object(
                  'runtime_sha256', encode(sha256(convert_to(value::text, 'UTF8')), 'hex')
                ))::text FROM payload;")
              ;;
            20260909180000)
              RUNTIME=$(psql "$DESTINO_URL" -X -v ON_ERROR_STOP=1 -At -c "
                WITH target_relation AS (
                  SELECT c.oid
                  FROM pg_class c
                  JOIN pg_namespace n ON n.oid = c.relnamespace
                  WHERE n.nspname='public' AND c.relname='global_settings'
                    AND c.relkind IN ('r','p')
                ), payload AS (
                  SELECT jsonb_build_object(
                    'server_major', current_setting('server_version_num')::int / 10000,
                    'database', current_database(),
                    'relation_count', (SELECT count(*) FROM target_relation),
                    'legacy_token_count', (
                      SELECT count(*) FROM public.global_settings WHERE key='api_token'
                    ),
                    'constraint_count', (
                      SELECT count(*) FROM pg_constraint
                      WHERE conrelid='public.global_settings'::regclass
                        AND conname='global_settings_no_plaintext_api_token'
                    ),
                    'validated_constraint_count', (
                      SELECT count(*) FROM pg_constraint
                      WHERE conrelid='public.global_settings'::regclass
                        AND conname='global_settings_no_plaintext_api_token'
                        AND convalidated
                    )
                  ) AS value
                )
                SELECT (value || jsonb_build_object(
                  'runtime_sha256', encode(sha256(convert_to(value::text, 'UTF8')), 'hex')
                ))::text FROM payload;")
              ;;
            20260909200000)
              RUNTIME=$(psql "$DESTINO_URL" -X -v ON_ERROR_STOP=1 -At \
                -f scripts/db-audit/inbox-contact-authorization-runtime.sql)
              ;;
            20260909210000)
              RUNTIME=$(psql "$DESTINO_URL" -X -v ON_ERROR_STOP=1 -At \
                -f scripts/db-audit/talkx-template-history-runtime.sql)
              ;;
            *)
              echo "::error::Migration sem contrato runtime permitido neste workflow."
              exit 1
              ;;
          esac
          printf '%s\n' "$RUNTIME"
          RUNTIME_JSON="$RUNTIME" node --input-type=module <<'NODE'
          const proof = JSON.parse(process.env.RUNTIME_JSON);
          const common = proof.server_major === 17
            && proof.database === 'postgres'
            && /^[a-f0-9]{64}$/.test(proof.runtime_sha256 || '');
          let targetOk = false;
          if (process.env.TARGET_VERSION === '20260830170000') {
            targetOk = proof.function_count === 1
              && proof.security_definer === true
              && proof.stable === true
              && proof.search_path_fixed === true
              && proof.filters_active === true;
          } else if (process.env.TARGET_VERSION === '20260831120000') {
            const base = proof.table_count === 1
              && proof.rls_enabled === true
              && proof.policy_count === 1
              && proof.service_role_crud === true;
            targetOk = process.env.NEEDS_APPLY === 'true'
              ? base
                && proof.public_policy_count === 1
                && proof.service_role_policy_count === 0
                && proof.anon_acl === true
                && proof.authenticated_acl === true
              : base
                && proof.public_policy_count === 0
                && proof.service_role_policy_count === 1
                && proof.anon_acl === false
                && proof.authenticated_acl === false
                && proof.service_role_extra === false;
          } else if (process.env.TARGET_VERSION === '20260831150000') {
            targetOk = proof.source_count === 4
              && Number.isInteger(proof.legacy_signed_url_count)
              && proof.legacy_signed_url_count >= 0
              && (process.env.NEEDS_APPLY === 'true' || proof.legacy_signed_url_count === 0);
          } else if (process.env.TARGET_VERSION === '20260908180000') {
            targetOk = process.env.NEEDS_APPLY === 'true'
              ? proof.table_count === 0
                && proof.rls_count === 0
                && proof.function_count === 0
                && proof.safe_function_count === 0
                && proof.trigger_count === 0
              : proof.table_count === 2
                && proof.rls_count === 2
                && proof.function_count === 6
                && proof.safe_function_count === 6
                && proof.trigger_count === 1;
          } else if (process.env.TARGET_VERSION === '20260908220000') {
            targetOk = process.env.NEEDS_APPLY === 'true'
              ? proof.lease_column_count === 0
                && proof.new_complete_count === 0
                && proof.old_complete_count === 1
                && proof.new_fail_count === 0
                && proof.state_constraint_count === 0
              : proof.lease_column_count === 1
                && proof.new_complete_count === 1
                && proof.old_complete_count === 0
                && proof.new_fail_count === 1
                && proof.state_constraint_count === 3;
          } else if (process.env.TARGET_VERSION === '20260909120000') {
            const base = proof.table_count === 2
              && proof.constraint_count === 5
              && proof.flag_enabled === false;
            targetOk = process.env.NEEDS_APPLY === 'true'
              ? base
                && proof.function_count === 0
                && proof.flag_count === 0
              : base
                && proof.validated_constraint_count === 5
                && proof.function_count === 3
                && proof.safe_function_count === 3
                && proof.flag_count === 1;
          } else if (process.env.TARGET_VERSION === '20260909180000') {
            const base = proof.relation_count === 1
              && Number.isInteger(proof.legacy_token_count)
              && proof.legacy_token_count >= 0
              && proof.legacy_token_count <= 1;
            targetOk = process.env.NEEDS_APPLY === 'true'
              ? base
                && proof.constraint_count === 0
                && proof.validated_constraint_count === 0
              : base
                && proof.legacy_token_count === 0
                && proof.constraint_count === 1
                && proof.validated_constraint_count === 1;
          } else if (process.env.TARGET_VERSION === '20260909200000') {
            const base = proof.note_table_count === 1
              && proof.function_count === 4
              && proof.note_rls_enabled === true
              && Number.isInteger(proof.policy_count)
              && proof.policy_count >= 0;
            targetOk = process.env.NEEDS_APPLY === 'true'
              ? base
              : base
                && proof.safe_function_count === 4
                && proof.definition_sha256 === '55078d2e365413bea0b93777ec63e70063def445760d0ae8f516670b86070771'
                && proof.caller_bound_count === 3
                && proof.rpc_authorized_count === 1
                && proof.policy_count === 4
                && proof.policy_signature_count === 4
                && proof.identity_trigger_count === 1
                && proof.authenticated_api_execute_count === 4
                && proof.anon_api_execute_count === 0
                && proof.guard_authenticated_execute === false
                && proof.guard_anon_execute === false
                && proof.anon_note_access === false
                && proof.authenticated_note_crud === true
                && proof.authenticated_note_extra === false
                && proof.service_note_crud === true
                && proof.service_note_extra === false;
          } else if (process.env.TARGET_VERSION === '20260909210000') {
            const base = proof.table_count === 1
              && proof.variant_table_count === 1
              && proof.custom_variables_column_count === 1
              && proof.invalid_live_template_count === 0
              && proof.invalid_variant_count === 0
              && proof.variant_rls_enabled === true
              && proof.recipient_variant_fk_count === 1
              && proof.rls_enabled === true;
            targetOk = process.env.NEEDS_APPLY === 'true'
              ? base
                && (
                  (proof.variant_policy_count === 2
                   && proof.variant_legacy_write_policy_count === 1
                   && proof.variant_canonical_policy_signature_count === 0
                   && proof.variant_validated_constraint_count === 0)
                  ||
                  (proof.variant_policy_count === 4
                   && proof.variant_legacy_write_policy_count === 0
                   && proof.variant_canonical_policy_signature_count === 4
                   && proof.variant_validated_constraint_count === 3
                   && proof.variant_anon_any_access === false
                   && proof.variant_authenticated_crud === true
                   && proof.variant_authenticated_extra_access === false
                   && proof.variant_service_role_crud === true
                   && proof.variant_service_role_extra_access === false)
                )
                && [0, 1].includes(proof.history_description_column_count)
                && [1, 2].includes(proof.policy_count)
                && [3, 4].includes(proof.foundation_constraint_count)
                && proof.function_count === 0
                && proof.safe_function_count === 0
                && proof.constraint_count === 0
                && proof.immutable_trigger_count === 0
                && proof.template_update_guard_count === 0
                && proof.template_validation_trigger_count === 0
                && proof.template_timestamp_trigger_count === 0
                && proof.anon_any_access === true
                && proof.authenticated_select === true
                && proof.authenticated_any_mutation === true
                && proof.authenticated_rpc_execute === false
                && proof.authenticated_internal_function_execute_count === 0
              : base
                && proof.variant_policy_count === 4
                && proof.variant_legacy_write_policy_count === 0
                && proof.variant_canonical_policy_signature_count === 4
                && proof.variant_validated_constraint_count === 3
                && proof.variant_anon_any_access === false
                && proof.variant_authenticated_crud === true
                && proof.variant_authenticated_extra_access === false
                && proof.variant_service_role_crud === true
                && proof.variant_service_role_extra_access === false
                && proof.history_description_column_count === 1
                && proof.policy_count === 1
                && proof.foundation_constraint_count === 4
                && proof.canonical_select_policy_count === 1
                && proof.constraint_count === 3
                && proof.validated_constraint_count === 3
                && proof.function_count === 6
                && proof.safe_function_count === 6
                && proof.definition_sha256 === '079e2bd466e89e58251c7d14596453256cade54b9f0eddd4ce6df86ae751c2be'
                && proof.immutable_trigger_count === 1
                && proof.template_update_guard_count === 1
                && proof.template_validation_trigger_count === 1
                && proof.template_timestamp_trigger_count === 1
                && proof.anon_any_access === false
                && proof.authenticated_select === true
                && proof.authenticated_any_mutation === false
                && proof.history_service_role_crud === true
                && proof.history_service_role_extra_access === false
                && proof.authenticated_rpc_execute === true
                && proof.anon_rpc_execute === false
                && proof.authenticated_counter_execute === true
                && proof.anon_counter_execute === false
                && proof.authenticated_guard_execute === false
                && proof.authenticated_update_guard_execute === false
                && proof.authenticated_internal_function_execute_count === 0;
          }
          const ok = common && targetOk;
          if (!ok) {
            console.error('Estado runtime inesperado; migration cancelada.');
            process.exit(1);
          }
          if (process.env.APPLY === 'true'
              && proof.runtime_sha256 !== process.env.CONFIRM_RUNTIME_SHA256) {
            console.error('SHA-256 runtime mudou desde o dry-run; migration cancelada.');
            process.exit(1);
          }
          NODE

      - name: Dry-run oficial do Supabase CLI
        if: steps.preflight.outputs.needs_apply == 'true'
        env:
          DESTINO_URL: ${{ secrets.DESTINO_URL }}
        run: >-
          supabase db push --db-url "$DESTINO_URL" --include-all
          --skip-vault --dry-run --yes

      - name: Aplicar unica migration ausente
        if: inputs.apply == true && steps.preflight.outputs.needs_apply == 'true'
        env:
          DESTINO_URL: ${{ secrets.DESTINO_URL }}
        run: >-
          supabase db push --db-url "$DESTINO_URL" --include-all
          --skip-vault --yes

      - name: Validar registro no ledger
        if: inputs.apply == true || steps.preflight.outputs.needs_apply == 'false'
        env:
          DESTINO_URL: ${{ secrets.DESTINO_URL }}
          TARGET_VERSION: ${{ inputs.migration_version }}
          TARGET_NAME: ${{ steps.preflight.outputs.target_name }}
        run: |
          LEDGER_COUNT=$(psql "$DESTINO_URL" -X -v ON_ERROR_STOP=1 \
            -v target_version="$TARGET_VERSION" -v target_name="$TARGET_NAME" -At <<'SQL'
              SELECT count(*)
              FROM supabase_migrations.schema_migrations
              WHERE version = :'target_version'
                AND name = :'target_name';
          SQL
          )
          test "$LEDGER_COUNT" = "1"
          echo "OK: ledger possui exatamente version=$TARGET_VERSION name=$TARGET_NAME."

      - name: Validar comportamento de get_team_profiles
        if: >-
          (inputs.apply == true || steps.preflight.outputs.needs_apply == 'false')
          && inputs.migration_version == '20260830170000'
        env:
          DESTINO_URL: ${{ secrets.DESTINO_URL }}
        run: |
          RESULT=$(psql "$DESTINO_URL" -X -v ON_ERROR_STOP=1 -At -c "
            SELECT json_build_object(
              'returned_inactive', (
                SELECT count(*) FROM public.get_team_profiles()
                WHERE is_active IS DISTINCT FROM true
              ),
              'filters_active', position(
                'where p.is_active = true' in lower(
                  pg_get_functiondef('public.get_team_profiles()'::regprocedure)
                )
              ) > 0
            )::text;")
          printf '%s\n' "$RESULT"
          RESULT_JSON="$RESULT" node --input-type=module <<'NODE'
          const proof = JSON.parse(process.env.RESULT_JSON);
          if (proof.returned_inactive !== 0 || proof.filters_active !== true) process.exit(1);
          NODE

      - name: Validar ACL de webhook_failures
        if: >-
          (inputs.apply == true || steps.preflight.outputs.needs_apply == 'false')
          && inputs.migration_version == '20260831120000'
        env:
          DESTINO_URL: ${{ secrets.DESTINO_URL }}
        run: >-
          psql "$DESTINO_URL" -X -v ON_ERROR_STOP=1
          -f scripts/db-audit/check-webhook-failures-acl.sql

      - name: Validar remocao de URLs assinadas persistidas
        if: >-
          (inputs.apply == true || steps.preflight.outputs.needs_apply == 'false')
          && inputs.migration_version == '20260831150000'
        env:
          DESTINO_URL: ${{ secrets.DESTINO_URL }}
        run: |
          LEGACY_COUNT=$(psql "$DESTINO_URL" -X -v ON_ERROR_STOP=1 -At <<'SQL'
            SELECT sum(legacy_count)
            FROM (
              SELECT count(*) FILTER (WHERE media_url ~ '/storage/v1/object/sign/(audio-messages|team-chat-files|whatsapp-media)/') AS legacy_count FROM public.messages
              UNION ALL
              SELECT count(*) FILTER (WHERE media_url ~ '/storage/v1/object/sign/(audio-messages|team-chat-files|whatsapp-media)/') FROM public.scheduled_messages
              UNION ALL
              SELECT count(*) FILTER (WHERE media_url ~ '/storage/v1/object/sign/(audio-messages|team-chat-files|whatsapp-media)/') FROM public.team_messages
              UNION ALL
              SELECT count(*) FILTER (WHERE file_url ~ '/storage/v1/object/sign/(audio-messages|team-chat-files|whatsapp-media)/') FROM public.knowledge_base_files
            ) AS source_counts;
          SQL
          )
          test "$LEGACY_COUNT" = "0"
          echo "OK: nenhuma credencial temporaria de Storage permanece nas tabelas alvo."

      - name: Validar estrutura e privilegios da outbox CRM
        if: >-
          (inputs.apply == true || steps.preflight.outputs.needs_apply == 'false')
          && inputs.migration_version == '20260908180000'
        env:
          DESTINO_URL: ${{ secrets.DESTINO_URL }}
        run: |
          RESULT=$(psql "$DESTINO_URL" -X -v ON_ERROR_STOP=1 -At <<'SQL'
            SELECT jsonb_build_object(
              'outbox_rls', (SELECT relrowsecurity FROM pg_class WHERE oid = 'public.crm_sync_outbox'::regclass),
              'links_rls', (SELECT relrowsecurity FROM pg_class WHERE oid = 'public.crm_contact_links'::regclass),
              'auth_outbox_select', has_table_privilege('authenticated', 'public.crm_sync_outbox', 'SELECT'),
              'auth_outbox_mutate', has_table_privilege('authenticated', 'public.crm_sync_outbox', 'INSERT, UPDATE, DELETE'),
              'anon_outbox_access', has_table_privilege('anon', 'public.crm_sync_outbox', 'SELECT, INSERT, UPDATE, DELETE'),
              'auth_claim_execute', has_function_privilege('authenticated', 'public.claim_crm_sync_outbox(text,integer)', 'EXECUTE'),
              'service_claim_execute', has_function_privilege('service_role', 'public.claim_crm_sync_outbox(text,integer)', 'EXECUTE'),
              'trigger_count', (
                SELECT count(*) FROM pg_trigger
                WHERE tgrelid = 'public.conversation_closures'::regclass
                  AND tgname = 'trg_enqueue_crm_sync_from_closure' AND NOT tgisinternal
              )
            )::text;
          SQL
          )
          printf '%s\n' "$RESULT"
          RESULT_JSON="$RESULT" node --input-type=module <<'NODE'
          const proof = JSON.parse(process.env.RESULT_JSON);
          const ok = proof.outbox_rls === true
            && proof.links_rls === true
            && proof.auth_outbox_select === true
            && proof.auth_outbox_mutate === false
            && proof.anon_outbox_access === false
            && proof.auth_claim_execute === false
            && proof.service_claim_execute === true
            && proof.trigger_count === 1;
          if (!ok) process.exit(1);
          NODE

      - name: Validar fencing e maquina de estados da outbox CRM
        if: >-
          (inputs.apply == true || steps.preflight.outputs.needs_apply == 'false')
          && inputs.migration_version == '20260908220000'
        env:
          DESTINO_URL: ${{ secrets.DESTINO_URL }}
        run: |
          RESULT=$(psql "$DESTINO_URL" -X -v ON_ERROR_STOP=1 -At <<'SQL'
            SELECT jsonb_build_object(
              'lease_column', EXISTS (
                SELECT 1 FROM information_schema.columns
                WHERE table_schema='public' AND table_name='crm_sync_outbox' AND column_name='lease_token'
              ),
              'old_complete_absent', to_regprocedure('public.complete_crm_sync_outbox(uuid,text,text,text)') IS NULL,
              'new_complete_service', has_function_privilege(
                'service_role', 'public.complete_crm_sync_outbox(uuid,uuid,text,text,text)', 'EXECUTE'
              ),
              'new_complete_auth', has_function_privilege(
                'authenticated', 'public.complete_crm_sync_outbox(uuid,uuid,text,text,text)', 'EXECUTE'
              ),
              'contact_fk_delete_action', (
                SELECT confdeltype FROM pg_constraint
                WHERE conrelid='public.crm_sync_outbox'::regclass
                  AND conname='crm_sync_outbox_contact_id_fkey'
              ),
              'state_constraints', (
                SELECT count(*) FROM pg_constraint
                WHERE conrelid='public.crm_sync_outbox'::regclass
                  AND conname IN ('crm_sync_outbox_lease_state','crm_sync_outbox_success_state','crm_sync_outbox_phone_state')
              )
            )::text;
          SQL
          )
          printf '%s\n' "$RESULT"
          RESULT_JSON="$RESULT" node --input-type=module <<'NODE'
          const proof = JSON.parse(process.env.RESULT_JSON);
          if (!(proof.lease_column === true && proof.old_complete_absent === true
            && proof.new_complete_service === true && proof.new_complete_auth === false
            && proof.contact_fk_delete_action === 'n' && proof.state_constraints === 3)) process.exit(1);
          NODE

      - name: Validar rollout seguro, ACL e funcoes atomicas do CRM
        if: >-
          (inputs.apply == true || steps.preflight.outputs.needs_apply == 'false')
          && inputs.migration_version == '20260909120000'
        env:
          DESTINO_URL: ${{ secrets.DESTINO_URL }}
        run: |
          RESULT=$(psql "$DESTINO_URL" -X -v ON_ERROR_STOP=1 -At <<'SQL'
            WITH expected_functions AS (
              SELECT p.oid, p.proname, p.prosecdef,
                     COALESCE('search_path=public, pg_temp' = ANY(p.proconfig), false) AS safe_path
              FROM pg_proc p JOIN pg_namespace n ON n.oid=p.pronamespace
              WHERE n.nspname='public' AND p.proname IN (
                'merge_contacts_atomic','upsert_crm_contact_link_guarded','cleanup_crm_sync_outbox'
              )
            )
            SELECT jsonb_build_object(
              'validated_constraints', (
                SELECT count(*) FROM pg_constraint
                WHERE conrelid='public.crm_sync_outbox'::regclass AND convalidated
                  AND conname IN (
                    'crm_sync_outbox_payload_size','crm_sync_outbox_external_ids_bounded',
                    'crm_sync_outbox_lease_state','crm_sync_outbox_success_state',
                    'crm_sync_outbox_phone_state'
                  )
              ),
              'safe_functions', (SELECT count(*) FROM expected_functions WHERE prosecdef AND safe_path),
              'auth_links_select', has_table_privilege('authenticated','public.crm_contact_links','SELECT'),
              'auth_links_mutate',
                has_table_privilege('authenticated','public.crm_contact_links','INSERT') OR
                has_table_privilege('authenticated','public.crm_contact_links','UPDATE') OR
                has_table_privilege('authenticated','public.crm_contact_links','DELETE') OR
                has_table_privilege('authenticated','public.crm_contact_links','TRUNCATE'),
              'anon_links_access',
                has_table_privilege('anon','public.crm_contact_links','SELECT') OR
                has_table_privilege('anon','public.crm_contact_links','INSERT') OR
                has_table_privilege('anon','public.crm_contact_links','UPDATE') OR
                has_table_privilege('anon','public.crm_contact_links','DELETE') OR
                has_table_privilege('anon','public.crm_contact_links','TRUNCATE'),
              'auth_merge', has_function_privilege(
                'authenticated','public.merge_contacts_atomic(uuid,uuid[],jsonb)','EXECUTE'
              ),
              'anon_merge', has_function_privilege(
                'anon','public.merge_contacts_atomic(uuid,uuid[],jsonb)','EXECUTE'
              ),
              'service_guarded_link', has_function_privilege(
                'service_role','public.upsert_crm_contact_link_guarded(uuid,text,text,text)','EXECUTE'
              ),
              'auth_guarded_link', has_function_privilege(
                'authenticated','public.upsert_crm_contact_link_guarded(uuid,text,text,text)','EXECUTE'
              ),
              'service_cleanup', has_function_privilege(
                'service_role','public.cleanup_crm_sync_outbox(integer,integer,integer)','EXECUTE'
              ),
              'auth_cleanup', has_function_privilege(
                'authenticated','public.cleanup_crm_sync_outbox(integer,integer,integer)','EXECUTE'
              ),
              'delete_trigger_count', (
                SELECT count(*) FROM pg_trigger
                WHERE tgrelid='public.contacts'::regclass
                  AND tgname='trg_redact_crm_sync_on_contact_delete' AND NOT tgisinternal
              ),
              'runtime_flag_disabled', (
                SELECT enabled IS FALSE FROM public.feature_flags WHERE key='crm.integration'
              )
            )::text;
          SQL
          )
          printf '%s\n' "$RESULT"
          RESULT_JSON="$RESULT" node --input-type=module <<'NODE'
          const proof = JSON.parse(process.env.RESULT_JSON);
          const ok = proof.validated_constraints === 5
            && proof.safe_functions === 3
            && proof.auth_links_select === true
            && proof.auth_links_mutate === false
            && proof.anon_links_access === false
            && proof.auth_merge === true
            && proof.anon_merge === false
            && proof.service_guarded_link === true
            && proof.auth_guarded_link === false
            && proof.service_cleanup === true
            && proof.auth_cleanup === false
            && proof.delete_trigger_count === 1
            && proof.runtime_flag_disabled === true;
          if (!ok) process.exit(1);
          NODE

      - name: Validar kill switch persistente da API publica
        if: >-
          (inputs.apply == true || steps.preflight.outputs.needs_apply == 'false')
          && inputs.migration_version == '20260909180000'
        env:
          DESTINO_URL: ${{ secrets.DESTINO_URL }}
        run: |
          RESULT=$(psql "$DESTINO_URL" -X -v ON_ERROR_STOP=1 -At <<'SQL'
            SELECT jsonb_build_object(
              'legacy_token_count', (
                SELECT count(*) FROM public.global_settings WHERE key='api_token'
              ),
              'constraint_count', (
                SELECT count(*) FROM pg_constraint
                WHERE conrelid='public.global_settings'::regclass
                  AND conname='global_settings_no_plaintext_api_token'
                  AND convalidated
                  AND pg_get_constraintdef(oid) LIKE '%key <>%api_token%'
              )
            )::text;
          SQL
          )
          printf '%s\n' "$RESULT"
          RESULT_JSON="$RESULT" node --input-type=module <<'NODE'
          const proof = JSON.parse(process.env.RESULT_JSON);
          if (proof.legacy_token_count !== 0 || proof.constraint_count !== 1) process.exit(1);
          NODE

      - name: Validar autorizacao canonica do Inbox
        if: >-
          (inputs.apply == true || steps.preflight.outputs.needs_apply == 'false')
          && inputs.migration_version == '20260909200000'
        env:
          DESTINO_URL: ${{ secrets.DESTINO_URL }}
        run: |
          RESULT=$(psql "$DESTINO_URL" -X -v ON_ERROR_STOP=1 -At \
            -f scripts/db-audit/inbox-contact-authorization-runtime.sql)
          printf '%s\n' "$RESULT"
          RESULT_JSON="$RESULT" node --input-type=module <<'NODE'
          const proof = JSON.parse(process.env.RESULT_JSON);
          const ok = proof.server_major === 17
            && proof.database === 'postgres'
            && /^[a-f0-9]{64}$/.test(proof.runtime_sha256 || '')
            && proof.function_count === 4
            && proof.safe_function_count === 4
            && proof.definition_sha256 === '55078d2e365413bea0b93777ec63e70063def445760d0ae8f516670b86070771'
            && proof.note_rls_enabled === true
            && proof.caller_bound_count === 3
            && proof.rpc_authorized_count === 1
            && proof.policy_count === 4
            && proof.policy_signature_count === 4
            && proof.identity_trigger_count === 1
            && proof.authenticated_api_execute_count === 4
            && proof.anon_api_execute_count === 0
            && proof.guard_authenticated_execute === false
            && proof.guard_anon_execute === false
            && proof.anon_note_access === false
            && proof.authenticated_note_crud === true
            && proof.authenticated_note_extra === false
            && proof.service_note_crud === true
            && proof.service_note_extra === false;
          if (!ok) process.exit(1);
          NODE

      - name: Validar histórico canônico de templates Talk X
        if: >-
          (inputs.apply == true || steps.preflight.outputs.needs_apply == 'false')
          && inputs.migration_version == '20260909210000'
        env:
          DESTINO_URL: ${{ secrets.DESTINO_URL }}
        run: |
          RESULT=$(psql "$DESTINO_URL" -X -v ON_ERROR_STOP=1 -At \
            -f scripts/db-audit/talkx-template-history-runtime.sql)
          printf '%s\n' "$RESULT"
          RESULT_JSON="$RESULT" node --input-type=module <<'NODE'
          const proof = JSON.parse(process.env.RESULT_JSON);
          const ok = proof.server_major === 17
            && proof.database === 'postgres'
            && /^[a-f0-9]{64}$/.test(proof.runtime_sha256 || '')
            && proof.table_count === 1
            && proof.variant_table_count === 1
            && proof.custom_variables_column_count === 1
            && proof.invalid_live_template_count === 0
            && proof.invalid_variant_count === 0
            && proof.variant_rls_enabled === true
            && proof.variant_policy_count === 4
            && proof.variant_legacy_write_policy_count === 0
            && proof.variant_canonical_policy_signature_count === 4
            && proof.variant_validated_constraint_count === 3
            && proof.recipient_variant_fk_count === 1
            && proof.variant_anon_any_access === false
            && proof.variant_authenticated_crud === true
            && proof.variant_authenticated_extra_access === false
            && proof.variant_service_role_crud === true
            && proof.variant_service_role_extra_access === false
            && proof.history_description_column_count === 1
            && proof.rls_enabled === true
            && proof.policy_count === 1
            && proof.canonical_select_policy_count === 1
            && proof.constraint_count === 3
            && proof.validated_constraint_count === 3
            && proof.foundation_constraint_count === 4
            && proof.function_count === 6
            && proof.safe_function_count === 6
            && proof.definition_sha256 === '079e2bd466e89e58251c7d14596453256cade54b9f0eddd4ce6df86ae751c2be'
            && proof.immutable_trigger_count === 1
            && proof.template_update_guard_count === 1
            && proof.template_validation_trigger_count === 1
            && proof.template_timestamp_trigger_count === 1
            && proof.anon_any_access === false
            && proof.authenticated_select === true
            && proof.authenticated_any_mutation === false
            && proof.history_service_role_crud === true
            && proof.history_service_role_extra_access === false
            && proof.authenticated_rpc_execute === true
            && proof.anon_rpc_execute === false
            && proof.authenticated_counter_execute === true
            && proof.anon_counter_execute === false
            && proof.authenticated_guard_execute === false
            && proof.authenticated_update_guard_execute === false
            && proof.authenticated_internal_function_execute_count === 0;
          if (!ok) process.exit(1);
          NODE

      - name: Provar paridade integral apos aplicacao
        if: inputs.apply == true
        env:
          DESTINO_URL: ${{ secrets.DESTINO_URL }}
        run: node scripts/db-audit/check-migration-drift.mjs
